# Cyrius Field Notes — Index
# Practical lessons from building real software in Cyrius.
# Written during the cyrius-doom, bsp, and AGNOS kernel development sessions.
# Not a reference manual. These are the things you learn by doing.
#
# Verified on Cyrius 2.2→4.8.5. Compiler co-evolved with the projects.

# ─── cyrius-doom Engine (doom.toml) ─── 11 entries ──────────────────
# Building cyrius-doom (v0.19.0→v0.24.5) and the BSP geometry library.

# field_notes_overview        — What it's like to build real software in Cyrius right now
# fixed_point_reality         — Practical lessons from 16.16 fixed-point math
# wad_parsing_resilience      — WAD parser handles 1000 random malformed inputs without crashing
# bsp_as_library              — Designing a reusable library with zero globals and pure functions
# fuzz_finds_real_bugs        — Three real bugs found by fuzzing in under 10 seconds
# doom_as_compiler_test       — Why DOOM is the perfect stress test for a young compiler
# the_palette_double_alloc    — Lazy init vs init order — a subtle null pointer bug
# walls_on_screen             — The moment it worked — 46,110 pixels, 320 columns, E1M1 rendered
# what_i_learned_building_doom — Personal reflection on building DOOM in Cyrius
# the_session_in_numbers      — Complete build log from scaffolding to v0.19.0
# the_second_session          — Continuing cyrius-doom from v0.19.1 to v0.24.5

# ─── Language Gotchas (language.toml) ─── 6 entries ──────────────────
# Universal Cyrius constraints and patterns — applies to all programs.

# the_gvar_wall               — The initialized global variable limit (256 slots)
# the_256kb_ceiling            — cc2's output buffer limit and static array hazards
# the_preprocessed_line_number_problem — Error messages report preprocessed line numbers
# heap_allocation_patterns     — Structuring heap data without structs-on-heap
# enum_as_namespace            — Using enums as zero-cost namespaced constants
# the_logical_shift_trap       — >> is LOGICAL not arithmetic — the invisible wall bug

# ─── Kernel (kernel.toml) ─── 1 entry ───────────────────────────────
# AGNOS kernel hardening and buffer overflow discoveries.

# the_143kb_lie               — How 14 buffer overflows made a kernel look smaller than it was

# ─── Compiler & Toolchain (compiler.toml) ─── 11 entries ────────────
# cc2→cc3→cc4 arc: regressions, conventions, retrospectives, handoffs.

# when_cyrius_gets_serious_about_math — Performance projection with real optimizations
# the_road_to_three            — v3.0 retrospective: shipping a sovereign compiler
# r12_loop_var_regression_lesson — Loop-var-in-r12 opt that doubled render_frame time
# version_string_silent_drift  — --version stuck for 5 releases from a sed failure
# ship_now_swap_backend_later_pattern — Ship the API now via C shim, swap to native later
# stale_fixed_cap_pattern      — Endemic stale fixed-size-buffer caps worth a sweep
# silent_stdin_truncation_saga — mabda's 141KB bundle exposed cc3 silent truncation
# dist_bundle_stdlib_dep_convention — yukti-style single-file dist/<name>.cyr convention
# v4_0_0_reflections           — Personal observations on the 3.6→4.0 arc
# dce_cfg_research_roundup_440 — v4.4.0 research: tcc/chibicc/QBE/ldisasm/PIC
# handoff_at_4_8_5             — Opinion piece wrapping the 4.8.x cleanup ramp

# ─── Agent Perspective — Project (meta.toml) ─── 3 entries ──────────
# Genesis/meta agent: scope lessons, session retrospectives, cautionary tales.

# the_feel_of_the_language           — Subjective experience of writing Cyrius as an AI agent
# genesis_agent_first_cyrius         — Cautionary tale: writing without reading working programs
# genesis_agent_long_session_lessons — Tuesday session retrospective: temporal inflation, scope discipline, port-feedback loop
