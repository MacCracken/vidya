# Cyrius Compiler Implementation Patterns
# How each language feature is implemented in the compiler.
# An agent reading these entries can implement or extend any feature.

[[entries]]
name = "struct_implementation"
description = "How structs work: definition, initialization, field access, nested structs"
category = "cyrius"
content = '''
STORAGE:
  struct_names[32]   — name offset per struct (1-based ID)
  struct_fcounts[32] — field count per struct
  struct_fnames[32*16] — field name offsets (16 fields max per struct)
  struct_ftypes[32*16] — field type IDs (0=scalar, >0=struct_id for nested)
  var_types[256]     — per-variable struct type ID (>0=struct, <0=pointer scale)

DEFINITION: struct Point { x; y; }
  PARSE_STRUCT_DEF registers name, parses fields with optional `: TypeName`
  annotation for nested structs. All fields are 8 bytes unless nested.

INIT: var p = Point { 10, 20 };
  PARSE_STRUCT_INIT allocates var with size = STRUCTSZ (recursive for nested),
  then emits stores for each field at var_addr + FIELDOFF(struct, field_idx).
  For nested structs, flattens: Outer { 1, 20, 22, 99 } stores 4 values.

FIELD ACCESS: p.x
  PARSE_FIELD_LOAD: lookup var → get struct type → find field index →
  compute offset via FIELDOFF → emit mov rcx, <var_addr>; add rcx, offset;
  mov rax, [rcx]. Offsets folded at compile time for nested: o.inner.field
  = single add with combined offset.

FIELD STORE: p.x = 42;
  Same lookup, emit push rax (value); mov rcx, <addr+offset>; pop rax;
  mov [rcx], rax.
'''

[[entries]]
name = "typed_pointer_implementation"
description = "How typed pointers scale arithmetic: var p: *i64 = &buf; *(p+1)"
category = "cyrius"
content = '''
DECLARATION: var p: *i64 = &buf;
  In PARSE_VAR, after `:` check for `*` token. If found, parse type name
  (i64→8, i32→4, i16→2, i8→1). Store scale as negative value in var_types:
  SVTYPE(S, vcnt, 0 - pscale). For locals: SLTYPE(S, li, pscale).

TRACKING: ptr_scale state variable at heap offset 0x8F890.
  PARSE_FACTOR clears SPSC(S, 0) at start. When loading a typed pointer
  variable, sets SPSC(S, scale). After dereference (*ptr), clears to 0.

SCALING: p + 1 adds 8 bytes (not 1)
  In PARSE_EXPR, before EADDR (add), check GPSC(S). If > 1:
  EPTR_SCALE(S, ps) emits: mov rcx, rax; mov rax, ps; imul rcx
  This multiplies the offset by the pointer scale before adding.

CODEGEN: *(p + 1) for *i64
  1. Load p → rax (SPSC set to 8)
  2. Push rax
  3. Load 1 → rax
  4. EPTR_SCALE: multiply rax by 8 → rax = 8
  5. Pop p → rcx
  6. Add → rax = p + 8
  7. ELOAD64 → load 8 bytes from [rax]
  8. Clear SPSC
'''

[[entries]]
name = "logical_and_or_implementation"
description = "How && and || short-circuit evaluation works with extra patches"
category = "cyrius"
content = '''
TOKENS: && = 53, || = 54 (lexer handles double-char)

ECOND parses: expr OP expr [&& expr OP expr]* [|| expr OP expr]*

FOR &&: a == 1 && b == 2
  1. Parse first comparison (a == 1), emit cmp + inverted jcc → patch1
  2. See && token, call ECOND_AND
  3. Store patch1 in extra_patches array (ADDXP)
  4. Parse second comparison (b == 2), emit cmp + inverted jcc → patch2
  5. Return patch2 to caller
  6. Caller patches BOTH patch1 and patch2 to the false branch (PATCHXP)

FOR ||: a == 1 || b == 2
  1. Parse first comparison, emit NON-inverted jcc → true_patch
  2. See || token, call ECOND_OR
  3. Parse second comparison, emit inverted jcc → false_patch
  4. Patch true_patch to land HERE (start of body)
  5. Return false_patch to caller

CHAINING: a && b && c
  ECOND_AND is recursive: if another && follows, calls itself again.
  Each extra patch stored in array, all patched at the same false target.

HEAP: extra_patches[64] at 0x8F848, extra_patch_cnt at 0x8F888.
'''

[[entries]]
name = "for_loop_implementation"
description = "How for loops use token replay for the step expression"
category = "cyrius"
content = '''
SYNTAX: for (init; cond; step) { body }

PARSE_FOR:
  1. Parse init: call PARSE_STMT (handles var or assignment)
  2. Record loop_top = current code position
  3. Parse cond: call ECOND → get condition patch
  4. Save step tokens: record GTI, skip tokens until matching ), save range
  5. Parse body: consume { }, call PARSE_PROG
  6. REPLAY step: save current TI, rewind to step_start, parse as assignment
     (IDENT = EXPR without semicolon), restore TI
  7. Emit EJMP back to loop_top
  8. Patch condition false branch

STEP REPLAY: The step (i = i + 1) has no semicolon — it ends at ).
  Parse as: load noff (IDENT), consume =, PARSE_EXPR, then FINDLOCAL/FINDVAR
  to store. Must check locals first (GINFN) for correct stack slot.

BREAK: Uses loop_top (0x8F838) and break_patch (0x8F840) heap state.
  break emits EJMP0, stores patch. After loop, EPATCH to end.

CONTINUE: Emits EJMP to loop_top (condition check, skipping step in for loops).

VAR SCOPE: for (var i = 0; ...) declares i as a local. The compiler
  doesn't have block scoping — i persists in the function scope.
  Multiple for loops in the same function need different var names.
'''

[[entries]]
name = "break_continue_implementation"
description = "How break and continue work with loop state"
category = "cyrius"
content = '''
HEAP STATE:
  0x8F838  loop_top   — code position of current loop condition
  0x8F840  break_patch — placeholder offset for break jump (0 if none)

WHILE LOOP:
  Save loop_top/break_patch. Set loop_top = current CP. Set break_patch = 0.
  After body: EJMP to loop_top. EPATCH condition. If break_patch > 0,
  EPATCH it to current position (after loop).

BREAK: Emits EJMP0 (placeholder), stores offset in break_patch.
  Only ONE break per loop (last break wins). Nested loops save/restore state.

CONTINUE: Emits EJMP to loop_top. In while loops, jumps to condition.
  In for loops, jumps to condition (skipping step — known limitation).

NESTING: PARSE_WHILE saves saved_ltop and saved_brk at entry,
  restores at exit. Each nesting level has its own break/continue state.
'''

[[entries]]
name = "elif_implementation"
description = "How elif chains are implemented with recursive parsing"
category = "cyrius"
content = '''
PARSE_IF checks for elif (token 50) after the if body closes:

  if (cond1) { body1 }
  elif (cond2) { body2 }
  elif (cond3) { body3 }
  else { body4 }

CODEGEN:
  1. Emit cond1 check → patch1 (jumps to false if cond1 fails)
  2. Emit body1
  3. See elif → emit jmp-to-end (skip), patch1 to land here
  4. PARSE_ELIF_CHAIN: parse cond2 → patch2, emit body2
  5. See elif → recurse: emit jmp-to-end, patch2 to here
  6. Parse cond3 → patch3, emit body3
  7. See else → emit jmp-to-end, patch3 to here, emit body4
  8. Patch all jmp-to-end to land at final position

RECURSIVE: PARSE_ELIF_CHAIN calls itself for each elif. Each level
  emits a skip-jmp before patching the previous condition, ensuring
  only one branch executes.
'''

[[entries]]
name = "include_preprocessing"
description = "How include directives expand files before lexing"
category = "cyrius"
content = '''
PREPROCESS runs before LEX. Scans input for lines starting with "include ".

ALGORITHM:
  1. Walk input byte by byte
  2. Check ISINCLUDE: matches "include " (8 chars) via byte comparison
  3. Extract filename from quotes: include "path/to/file.cyr"
  4. Read file with READFILE(path, dst, maxsz) using syscall(2, open)
  5. Append file contents to output buffer at S+0x91000
  6. Skip to end of include line
  7. Non-include lines: copy byte to output
  8. After scanning: copy expanded output back to S+0 (input buffer)
  9. Update BL (input length) to expanded size

LIMITS: Input buffer = 131072 bytes. Include expansion must fit.
  Currently no recursive include support (includes in included files
  are NOT expanded). No include guards needed.

FILENAME: Extracted into temp at S+0x90000, null-terminated for syscall.
'''

[[entries]]
name = "inline_asm_mnemonic_dispatch"
description = "How inline asm handles both raw bytes and mnemonic instructions"
category = "cyrius"
content = '''
PARSE_STMT dispatches asm (token 48) to the asm block parser.

ASM BLOCK: asm { ... }
  Inside braces, each item is either:
  - NUM (type 1): emit as raw byte via EB(S, value)
  - IDENT (type 2): dispatch to ASM_MNEMONIC

ASM_MNEMONIC:
  Reads the identifier name, packs first 8 bytes as L64(S + 0x50000 + noff).
  Matches against known mnemonics using masked comparison:
    if (mnem & 0xFFFFFF == 0x696C63) → cli (3 bytes match)

  ORDER MATTERS: longer prefixes first to avoid collisions.
  "invlpg" starts with "in" — check invlpg before int before in.

  18 mnemonics implemented:
  No-operand: cli, sti, hlt, nop, iretq, wrmsr, rdmsr, cpuid, pushfq, popfq
  With operands: lgdt [rax], lidt [rax], invlpg [rax], ltr ax,
    mov cr0/cr3/cr4, rax / mov rax, cr0/cr3/cr4, int N, in al dx, out dx al

  Operand parsing: simple pattern matching (expect [, ident, ] for memory;
  ident, comma, ident for register pairs; NUM for immediates).
'''

[[entries]]
name = "gt6_params_implementation"
description = "How functions with more than 6 parameters use the stack"
category = "cyrius"
content = '''
System V AMD64 ABI: first 6 args in rdi, rsi, rdx, rcx, r8, r9.
Args 7+ are pushed on the stack by the caller.

CALLER (PARSE_FNCALL):
  1. Evaluate each arg, push onto stack
  2. Pop first 6 into registers (reverse order)
  3. Remaining args stay on stack
  4. Emit CALL (rel32 fixup)
  5. After call: adjust SP to remove stack args

CALLEE (PARSE_FN_DEF):
  1. Prologue: push rbx; push r12; push rbp; mov rbp, rsp; sub rsp, frame_size
  2. Store register params to local slots: mov [rbp-8], rdi etc.
  3. Stack params at [rbp+32], [rbp+40], etc. (above saved rbx + r12 + rbp + return addr)
  4. ESTOREPARM(pc, li, total_params) handles the addressing

FRAME LAYOUT (v1.10.0):
  [rbp+40] = param 8 (7th, 0-indexed)
  [rbp+32] = param 7
  [rbp+24] = return address
  [rbp+16] = saved rbx
  [rbp+8]  = saved r12
  [rbp]    = saved rbp
  [rbp-8]  = local 0 / param 0
  [rbp-16] = local 1 / param 1
  ...
'''

[[entries]]
name = "two_pass_scanning"
description = "How the two-pass declaration scan enables global vars before/after functions"
category = "cyrius"
content = '''
PROBLEM: fn f() { return g; } var g = 42;
  Single-pass parsing: f references g before g is declared → error.

SOLUTION: Two-pass scanning in cc2.cyr main loop.

PASS 1 (register names, no codegen):
  - struct: PARSE_STRUCT_DEF (full parse, registers struct)
  - fn: skip body by counting braces (don't parse)
  - var: PARSE_GVAR_REG — register name+size, save TI, skip to semicolon
  - kernel: set kernel_mode flag
  - other: stop (start of main code)

PASS 2 (parse fn bodies):
  - struct: skip (already parsed)
  - fn: PARSE_FN_DEF (full parse — all globals now visible)
  - var: skip (already registered)
  - kernel: skip
  - other: stop

AFTER PASS 2:
  EMIT_GVAR_INITS: replay saved token positions, parse init expressions,
  emit stores. For struct inits, handle PARSE_STRUCT_INIT specially
  (detect struct name after =, emit field stores without re-registering).

HEAP: gvar_toks[512] at 0x8FA98, gvar_cnt at 0x8FC98.
'''

[[entries]]
name = "kernel_elf_emission"
description = "How kernel; directive produces a bootable multiboot1 ELF32"
category = "cyrius"
content = '''
DIRECTIVE: kernel; (token 56) sets kernel_mode flag at 0x8FCA0.

FIXUP changes: base address 0x400078 → 0x100060 for kernel mode.
  All variable/string addresses shift to kernel load address.

EMITELF_KERNEL produces ELF32 (not ELF64):
  - e_ident: ELFCLASS32, ELFDATA2LSB
  - e_machine: EM_386 (3) — required for multiboot compatibility
  - e_entry: 0x100060 (base + 52 ELF + 32 PH + 12 multiboot)
  - p_vaddr = p_paddr = 0x100000

  Layout: [ELF32 header 52B] [PH32 32B] [multiboot1 12B] [code] [data] [strings]

MULTIBOOT1 HEADER (12 bytes):
  magic:    0x1BADB002
  flags:    0x00000003 (page-align + memory info)
  checksum: -(magic + flags) & 0xFFFFFFFF = 0xE4524FFB

BOOT: qemu-system-x86_64 -kernel build/kernel -serial stdio
  (NOT qemu-system-i386 — needs x86_64 for long mode transition)
'''

[[entries]]
name = "isr_data_section_pattern"
description = "How interrupt service routines are built as bytecode in data buffers"
category = "cyrius"
content = '''
PROBLEM: ISR entry points cannot be Cyrius functions (prologue/epilogue wrong).
  Cyrius functions have: push rbp; mov rbp, rsp; sub rsp, N; ... ; ret
  ISRs need: save regs; handle; restore regs; iretq

SOLUTION: Build ISR bytecode in a data buffer at runtime.

PATTERN (timer_isr_build):
  var p = &timer_isr;
  store8(p, 0x50);       # push rax
  store8(p+1, 0x51);     # push rcx
  store8(p+2, 0x52);     # push rdx
  # mov rcx, &counter (48 B9 + 8-byte addr)
  store8(p+3, 0x48); store8(p+4, 0xB9);
  store64(p+5, &timer_ticks);  # RUNTIME address
  # mov rax, [rcx]; inc rax; mov [rcx], rax
  # EOI: mov al, 0x20; mov edx, 0x20; out dx, al
  # pop rdx; pop rcx; pop rax; iretq (48 CF)

IDT REGISTRATION:
  idt_set_gate(&idt + vector * 16, &timer_isr, 0x08, 0x8E);
  The buffer address (&timer_isr) is a DATA section address.
  Works because the PT_LOAD segment has RWX permissions.

KEY: store64(p+5, &timer_ticks) embeds the RUNTIME address.
  This is resolved by FIXUP at compile time, so the ISR code
  at runtime contains the correct absolute address.
'''

[[entries]]
name = "pic_timer_keyboard_pattern"
description = "How PIC remap, PIT timer, and keyboard interrupts are set up"
category = "cyrius"
content = '''
PIC REMAP (pic_init):
  Default: IRQ 0-7 → INT 8-15 (conflicts with CPU exceptions)
  Remap: IRQ 0-7 → INT 32-39, IRQ 8-15 → INT 40-47
  ICW1-ICW4 sequence via out instructions to ports 0x20/0xA0 (command)
  and 0x21/0xA1 (data). Set mask to enable only desired IRQs.

PIT TIMER:
  Channel 0, mode 2 (rate generator), ~100 Hz
  Command port 0x43: out 0x34 (channel 0, lobyte/hibyte, mode 2)
  Data port 0x40: out divisor low (0x9C), then high (0x2E)
  Divisor = 1193180 / 100 = 11932 = 0x2E9C
  Fires IRQ 0 → INT 32 after PIC remap

TIMER ISR: Read nothing, increment counter, send EOI (out 0x20, 0x20), iretq.
  EOI acknowledges the interrupt so PIC delivers the next one.

KEYBOARD ISR:
  Fires IRQ 1 → INT 33
  Read scancode: in al, 0x60
  Store in ring buffer: kb_buf[kb_head], increment kb_head & 0xFF
  Send EOI, iretq
  Ring buffer: kb_buf[256], kb_head, kb_tail
  API: kb_has_key() checks head != tail, kb_read_scancode() reads + advances tail
  scancode_to_ascii() maps PS/2 set 1 scancodes to ASCII

PIC MASK: 0xFC = enable IRQ0 (timer) + IRQ1 (keyboard), mask all others.
'''

[[entries]]
name = "floating_point_sse2"
description = "How f64 floating point works: SSE2 codegen, builtins, float literals"
category = "cyrius"
content = '''
DESIGN: "Everything is i64" — float values are IEEE 754 bit patterns stored in regular vars.
  Float operations are builtins (like syscall, load8) that emit SSE2 instructions.
  No type system changes needed. The programmer chooses float vs int operations explicitly.

BUILTINS (10 keywords, token types 62-71):
  f64_from(int)    — cvtsi2sd xmm0, rax; movq rax, xmm0 (int64 → double bits)
  f64_to(f64)      — movq xmm0, rax; cvttsd2si rax, xmm0 (double → int64, truncate)
  f64_add(a, b)    — addsd xmm0, xmm1
  f64_sub(a, b)    — subsd xmm0, xmm1
  f64_mul(a, b)    — mulsd xmm0, xmm1
  f64_div(a, b)    — divsd xmm0, xmm1
  f64_eq(a, b)     — ucomisd + sete → 0/1
  f64_lt(a, b)     — ucomisd + setb → 0/1
  f64_gt(a, b)     — ucomisd + seta → 0/1
  f64_neg(val)     — xorpd xmm1,xmm1; subsd xmm1,xmm0; movapd xmm0,xmm1

REGISTER PATTERN (binary ops):
  rax=b (second arg), stack=[a] (first arg pushed earlier)
  1. EPOPC → rcx=a, rax=b
  2. push rax (save b), mov rax,rcx (a→rax), movq xmm0,rax (a→xmm0)
  3. pop rax (restore b), mov rcx,rax (b→rcx), movq xmm1,rcx (b→xmm1)
  4. SSE op (xmm0 = result)
  5. movq rax, xmm0 (result back to rax for integer world)

FLOAT LITERALS: 3.14 lexed as token type 61.
  Value packed: (denominator << 32) | numerator. For 3.14: numer=314, denom=100.
  Codegen emits runtime conversion:
    mov rax, 314; cvtsi2sd xmm0, rax
    mov rcx, 100; cvtsi2sd xmm1, rcx
    divsd xmm0, xmm1; movq rax, xmm0

COMPARISON BUG (FIXED):
  xor eax,eax CLOBBERS ZF from ucomisd. sete always returns 1 after xor.
  Fix: use mov eax, 0 (B8 00000000) to clear rax without touching flags.

SSE2 INSTRUCTION ENCODINGS:
  movq xmm0, rax:     66 48 0F 6E C0
  movq xmm1, rcx:     66 48 0F 6E C9
  movq rax, xmm0:     66 48 0F 7E C0
  addsd xmm0, xmm1:   F2 0F 58 C1
  subsd xmm0, xmm1:   F2 0F 5C C1
  mulsd xmm0, xmm1:   F2 0F 59 C1
  divsd xmm0, xmm1:   F2 0F 5E C1
  cvtsi2sd xmm0, rax: F2 48 0F 2A C0
  cvttsd2si rax, xmm0: F2 48 0F 2C C0
  ucomisd xmm0, xmm1: 66 0F 2E C1
'''

[[entries]]
name = "methods_on_structs"
description = "Convention-based method dispatch: point.scale(2) → Point_scale(&point, 2)"
category = "cyrius"
content = '''
DESIGN: No new syntax for defining methods. Just a naming convention.
  Define: fn Point_scale(self, factor) { ... }
  Call:   point.scale(2)
  The compiler resolves point.scale(2) to Point_scale(&point, 2).

RESOLUTION:
  1. Parser sees ident.name( — the ( after dot-name triggers method dispatch
  2. FINDVAR(ident) → get struct type ID
  3. Look up struct name from struct_names[sid-1]
  4. Build mangled name: "Point" + "_" + "scale" in tok_names scratch area (offset 30000)
  5. FINDFN(mangled_name) → get function index
  6. Emit &var as first arg (self), then parse remaining args
  7. Emit normal function call to mangled name

BUILD_METHOD_NAME(S, si, mnoff):
  Uses tok_names scratch at offset 30000 to build "StructName_method\0"
  Copies struct name bytes, underscore (95), method name bytes, null terminator
  Returns offset 30000 for FINDFN lookup

STATEMENT CONTEXT:
  point.scale(2); as a statement needs special handling in PARSE_STMT.
  When parser sees ident.name in statement context, it peeks ahead:
    - If next is ( → method call: rewind, call PARSE_FIELD_LOAD, expect ;
    - Otherwise → field store: PARSE_FIELD_STORE

DUPLICATE VAR GOTCHA:
  No block scoping means var declarations in the method-call path and field-access
  path of the same function cannot reuse names like "si", "fi", "idx".
  Solution: declare shared vars once at function top, assign in branches.
'''

[[entries]]
name = "error_line_numbers"
description = "How line:column error reporting works in the compiler"
category = "cyrius"
content = '''
DESIGN: Parallel tok_lines array stores line number for each token.
  Line counter incremented during lexing on every newline (char 10).
  Error functions read line from tok_lines[token_index].

HEAP LAYOUT:
  tok_lines at S+0x1A2000 (65536 slots × 8 bytes = 524288 bytes)
  cur_line at S+0x8FCB8 (current line number during lexing)

LEXER TRACKING:
  In LEX() main loop:
    - Whitespace: if (c == 10) { SCLINE(S, GCLINE(S) + 1); }
    - Comments: same increment when hitting newline at end of # comment
  In ADDTOK(): STLINE(S, tc, GCLINE(S)) stores current line for each token

ERROR FORMAT:
  Old: "error at token 42 (type=5)"
  New: "error:3: unexpected token (type=5)"
  Warnings: "warning:15: assigning non-pointer"
  Duplicates: "error:7: duplicate variable"

INIT: cur_line starts at 1 (not 0) — S64(S + 0x8FCB8, 1) in compiler.cyr

PREPROCESSOR NOTE:
  Line numbers are for the EXPANDED source (after include processing).
  Included files reset nothing — lines count continuously.
  A future improvement would track file:line pairs.
'''

[[entries]]
name = "tok_names_overlap_bugs"
description = "Two tok_names overflow/overlap bugs: v1.5.2 (var_noffs corruption) and v1.7.5 (struct_ftypes corruption)"
category = "cyrius"
content = '''
BUG #1 (v1.5.2) — tok_names overflows into var_noffs:
  tok_names at 0x50000 (32768 bytes) overflowed into var_noffs at 0x58000.
  Fix: expanded tok_names to 65536 bytes (0x50000-0x60000), relocated var_noffs to 0x60000.

BUG #2 (v1.7.5) — tok_names overlaps struct_ftypes:
  struct_ftypes was placed at 0x59000 INSIDE the tok_names buffer (0x50000-0x60000).
  When programs had >36864 bytes of identifier data, tok_names wrote over struct_ftypes.
  Struct operations then zeroed out identifier strings, causing FINDVAR to fail silently.

  Symptoms: "unexpected +" at line 2556 when including assert.cyr + bench.cyr + all
  agnostik modules. Adding/removing ANY function near the boundary changed the error.
  Each individual library compiled fine alone — only the combination triggered it.

  Diagnosis: Added debug prints to dump var_noffs entries. Found var_noffs[340] had
  name offset 36868, which is 0x59004 = struct_ftypes + 4. The identifier "r" was
  being stored inside struct_ftypes and getting zeroed by struct initialization.

  Fix: relocated struct_ftypes from 0x59000 to 0x8A000 (free space after output_buf,
  before compiler state at 0x8C100). No overlap with tok_names.

LESSON: NEVER place data structures inside another buffer's address range, even if
  you think the outer buffer "won't grow that large." Add explicit non-overlap assertions
  or use separate memory regions with guard gaps.
'''

[[entries]]
name = "two_step_bootstrap"
description = "How to bootstrap heap layout changes without breaking the chain"
category = "cyrius"
content = '''
PROBLEM: When you change heap offsets (tok_values, var_noffs, preprocessor buffer),
  the committed cc2 binary uses the OLD layout. But the new source expects the NEW layout.
  If the old cc2 compiles the new source, the generated binary has correct offsets,
  but the old cc2 ITSELF may corrupt data during compilation if the old preprocessor
  buffer overlaps with the new token arrays.

TWO-STEP BOOTSTRAP:
  Step 1: Old cc2 compiles new source → cc3 (has new layout)
  Step 2: cc3 compiles same source → cc4 (verify cc3==cc4 byte-identical)
  Step 3: cp cc3 build/cc2 (update committed binary)
  Step 4: Now cc2 has the new layout and can compile any source

WHEN IT FAILS:
  If the old cc2 preprocessor buffer overlaps with the new token arrays,
  the old cc2 corrupts its own data during compilation of the new source.
  Solution: make the infrastructure change (buffer relocation) FIRST without
  adding new code, bootstrap to get a clean cc2, THEN add the new code.

MULTI-BACKEND GOTCHA:
  When relocating shared buffers (tok_values, var_noffs), ALL backend-specific
  files must be updated too. The aarch64/emit.cyr had its own TOKVAL function
  still reading from the old 0xE2000 while the shared lexer wrote to 0x122000.
  Every token value read as 0. All immediates compiled as MOVZ X0, #0.
  Fix: grep ALL source files for the old offset and update every occurrence.
'''

[[entries]]
name = "preprocessor_fix"
description = "How the include preprocessor was fixed to not eat string literals"
category = "cyrius"
content = '''
THE BUG:
  "include " appearing anywhere in source triggers file inclusion.
  A string literal like var s = "include this" causes the preprocessor
  to try opening a file named "this" and silently fail.

ROOT CAUSE:
  ISINCLUDE(S, ip, bl) checked if 8 bytes at position ip match "include ".
  Called on EVERY byte position in the source, not just line starts.

FIX:
  Track beginning-of-line (bol) state in PREPROCESS:
    var bol = 1;
    # Only check include at column 0
    var is_inc = 0;
    if (bol == 1) { is_inc = ISINCLUDE(S, ip, bl); }
    if (is_inc == 1) { ... handle include ... }
    else { copy byte; if (c == 10) { bol = 1; } else { bol = 0; } }

GOTCHA:
  The initial implementation used nested ifs: if (bol==1) { if (ISINCLUDE...) { } } else { }
  The else bound to the INNER if, not the outer. When bol==0, NEITHER branch executed,
  causing an infinite loop (ip never advanced). Fix: compute is_inc into a var first.
'''

[[entries]]
name = "hashmap_tombstone"
description = "Why delete-by-zeroing breaks open-addressing hashmaps and the tombstone fix"
category = "cyrius"
content = '''
THE BUG:
  hashmap.cyr used open addressing with linear probing.
  map_delete set occupied=0 (empty). This breaks probe chains:
    1. Insert "alpha" at slot 3
    2. Insert "beta" at slot 3 (collision) → probes to slot 4
    3. Delete "alpha" (slot 3 = empty)
    4. Lookup "beta" → hash to slot 3, see empty, stop → NOT FOUND (wrong!)

FIX — TOMBSTONE STATE:
  Entry state: 0=empty, 1=occupied, 2=tombstone (deleted)
  map_delete sets state=2 instead of state=0.
  _map_find skips tombstones (state==2) during probe, keeps searching.
  _map_find remembers first tombstone for INSERT (reuse deleted slots).
  map_get/map_has check state==1 (not state!=0 — tombstone is not occupied).
  _map_grow only rehashes state==1 entries (tombstones are garbage-collected).

RELATED CHECKS:
  map_set: count tombstone->occupied as new insertion (st != 1 increments count)
  hash_str: guard against null pointer (return 0)
'''

[[entries]]
name = "p1_hardening_pattern"
description = "P-1 hardening checklist for Cyrius stdlib libraries"
category = "cyrius"
content = '''
Before adding new features, audit existing code for robustness.
Checklist per function:

  1. NULL POINTER: Does the function dereference args without checking for 0?
     Fix: if (ptr == 0) { return 0; } or return error code.

  2. BOUNDS CHECK: Does the function index into arrays without validating?
     Fix: if (idx < 0 || idx >= len) { return -1; }

  3. DIVISION BY ZERO: Any division without checking divisor?
     Fix: if (divisor == 0) { return 0; }

  4. ALLOCATION FAILURE: Does alloc() return value get checked?
     Fix: var p = alloc(size); if (p == 0) { return 0; }

  5. BUFFER OVERFLOW: Does the function write without tracking position vs capacity?
     Fix: Add maxlen parameter or internal bounds check.

FOUND IN v0.9.3 AUDIT:
  - vec_remove: no bounds check on idx → arbitrary memory write
  - alloc: brk failure not checked → writes to unmapped memory
  - json_get: null key dereference → crash
  - fmt_sprintf: no output buffer bounds → overflow
  - hashmap delete: breaks probe chains → data loss

PRINCIPLE: Every library function that takes a pointer or index should
validate before dereferencing. Return 0 or -1 on invalid input.
'''

[[entries]]
name = "self_hosting_test"
description = "How to correctly test self-hosting in compiler.sh"
category = "cyrius"
content = '''
CORRECT TEST:
  cat src/main.cyr | build/cc2 > /tmp/cc3
  cmp build/cc2 /tmp/cc3
  # Must be byte-identical

WRONG TEST (was in compiler.sh):
  cat src/bridge.cyr | cc2 > cc2_copy
  cat src/bridge.cyr | cc2_copy > cc3
  cmp cc2_copy cc3
  # This tests bridge compiler self-hosting, not cc2 self-hosting.
  # cc2 was compiled from compiler.cyr, not cc_bridge.cyr.
  # Different source = different binary = always fails comparison.

PRINCIPLE: The self-hosting test must compile the SAME source that
produced the binary being tested. cc2 was built from compiler.cyr,
so the test must compile compiler.cyr with cc2 and compare.
'''

# ================================================================
# PLANNED FEATURES — Implementation strategies for v0.9.5+
# These entries describe HOW to implement each feature, not what it does.
# ================================================================

[[entries]]
name = "plan_block_scoping"
description = "Implementation strategy for block-scoped variables (scope depth tracking)"
category = "cyrius"
content = '''
DIFFICULTY: Very Low. No codegen changes. Compile-time only.

HEAP ADDITIONS (~2KB):
  scope_depth        [8]      — current nesting depth (0 = function top)
  fn_local_depths[64] [512]   — scope depth where each local was declared

ALGORITHM:
  1. When parsing { in function body: scope_depth += 1
  2. When PARSE_VAR declares a local: fn_local_depths[local_idx] = scope_depth
  3. When parsing }: scope_depth -= 1, then walk fn_local_names backward:
     while (fn_local_cnt > 0 && fn_local_depths[fn_local_cnt-1] > scope_depth) {
       fn_local_cnt -= 1;
     }
  4. Variable lookup (FINDLOCAL) unchanged — it only searches 0..fn_local_cnt

STACK FRAME:
  No change. All locals share the same stack frame (max locals * 8 bytes).
  Scope exit just removes names from the compile-time table.
  Stack space is NOT reclaimed (no benefit for single-frame functions).

GOTCHA:
  for-loop vars (var i = 0) need special handling — the i is in the for body scope.
  Currently for loops use token replay. The scope should open before the init var
  and close after the loop body.

PATTERN: Same as Crafting Interpreters chapter on locals. Runtime cost: zero.
'''

[[entries]]
name = "plan_enum_constructors"
description = "Implementation strategy for enum variants with data (auto-generated constructor functions)"
category = "cyrius"
content = '''
DIFFICULTY: Medium. Extends PARSE_ENUM_DEF to auto-generate functions.

CURRENT ENUM STATE:
  enum Color { RED = 0; GREEN = 1; BLUE = 2; }
  → Stored as named constants. No payload.

EXTENSION — ENUM WITH DATA:
  enum Result { Ok(val); Err(code); }
  → Each variant gets an auto-generated constructor function.

HEAP ADDITIONS (~2KB):
  enum_payloads[32*8]  [2048]  — 1 if variant has payload, 0 if not

PARSING:
  In PARSE_ENUM_DEF, after parsing variant name:
    if (PEEKT(S) == 10) {  # ( after variant name
      # This variant has a payload
      STI(S, GTI(S) + 1); # consume (
      # parse param name (store for constructor)
      STI(S, GTI(S) + 1); # consume param
      if (PEEKT(S) != 11) { ERR(S); }
      STI(S, GTI(S) + 1); # consume )
      enum_payloads[variant_idx] = 1;
    }

CONSTRUCTOR GENERATION:
  For each variant with payload, emit a function at compile time:
    fn Result_Ok(val) {
      var t = alloc(16);      # tag + payload
      store64(t, 0);          # tag = variant index
      store64(t + 8, val);    # payload
      return t;
    }
  Without payload:
    fn Result_None() {
      var t = alloc(16);
      store64(t, 0);
      store64(t + 8, 0);
      return t;
    }

  The constructor name is EnumName_VariantName (same mangling as methods).
  Register via REGFN, emit code into codebuf, record offset in fn_offsets.

USAGE:
  var r = Result_Ok(42);    # calls generated constructor
  var tag = load64(r);      # 0 = Ok
  var val = load64(r + 8);  # 42

INTEGRATION WITH tagged.cyr:
  The existing Some/None/Ok/Err functions in tagged.cyr ARE hand-written constructors.
  Auto-generation replaces them — any enum with data gets constructors for free.
'''

[[entries]]
name = "plan_pattern_matching"
description = "Implementation strategy for match expressions with destructuring"
category = "cyrius"
content = '''
DIFFICULTY: High. New keyword + codegen for destructuring binds.

SYNTAX:
  match expr {
    0 => { ... }
    42 => { ... }
    _ => { ... }
  }

  With destructuring (requires enum constructors):
  match result {
    Ok(v) => { use v }
    Err(e) => { use e }
  }

TOKEN: Add MATCH keyword (token type 72). ARROW => (token type 73).

CODEGEN (simple match — integer values):
  1. Evaluate match expression → rax
  2. Store in hidden temp var (like switch does)
  3. For each arm: compare tag, emit JNE to next arm
  4. Body of matching arm executes
  5. JMP to end after body
  Same as switch/case but with => instead of case/colon.

CODEGEN (destructuring match — tagged unions):
  1. Evaluate match expression → rax (pointer to tagged union)
  2. Load tag: load64(rax) → store in temp
  3. For each arm with pattern Ok(v):
     a. Compare tag with Ok variant index (e.g., 0)
     b. If match: load payload (load64(rax + 8)) into new local var v
     c. Execute body with v in scope (uses block scoping)
     d. JMP to end

HEAP ADDITIONS (~1KB):
  match_var_idx      [8]    — hidden var index for match subject
  match_arm_count    [8]    — number of arms parsed so far
  match_end_patches[32] [256] — JMP-to-end offsets for each arm

DEPENDENCY: Block scoping (for destructured variables in arm scope).
PATTERN: Compiles identically to switch + variable binding. The switch codegen
already exists (PARSE_SWITCH). Match extends it with destructuring.
'''

[[entries]]
name = "plan_closures"
description = "Implementation strategy for closures and lambda expressions"
category = "cyrius"
content = '''
DIFFICULTY: High. New syntax + heap-allocated capture struct + indirect calls.

SYNTAX:
  var f = |x| x + 1;        # single expression
  var g = |a, b| { ... };   # block body

REPRESENTATION (runtime):
  Closure = heap-allocated struct:
    offset 0: fn_ptr (code address of closure body)
    offset 8: capture_count (number of captured vars)
    offset 16+: captured values (i64 each, copied by value)

PARSING:
  New token PIPE (|) already exists as token 35.
  When PARSE_FACTOR sees |:
    1. Parse parameter list until next |
    2. Register anonymous function __closure_N
    3. Scan body for references to outer locals (capture analysis)
    4. Generate function body with captures as extra leading parameters
    5. Emit allocation of closure struct

CAPTURE ANALYSIS (simple):
  Walk tokens in closure body. For each IDENT token:
    if FINDLOCAL(outer_scope, name) succeeds → mark as captured
  This is a pre-scan, not a full parse. Works for single-pass.

CALLING A CLOSURE:
  Current fnptr.cyr uses fncall0/fncall1/fncall2 for indirect calls.
  Closure call:
    1. Load fn_ptr from closure: load64(closure_addr)
    2. Load captures: load64(closure_addr + 16), load64(closure_addr + 24)...
    3. Push captures as first N args, then actual args
    4. Emit indirect call: call rax (already supported)

CAPTURE BY VALUE:
  Cyrius has no GC or borrow checker. Closures COPY captured values.
  If the original variable changes after closure creation, the closure keeps the old value.
  This is safe and simple. Mutable captures require capture-by-reference (pointer),
  which is a future enhancement.

GOTCHA: Closures that outlive their creating scope need heap allocation.
  The bump allocator handles this (no free needed for short-lived closures).
'''

[[entries]]
name = "plan_module_system"
description = "Implementation strategy for modules with namespace and visibility"
category = "cyrius"
content = '''
DIFFICULTY: Medium. Extends preprocessor + parser with name mangling.

DESIGN: File-based modules. Each .cyr file is a module. Directory = submodule.
  mod math;           → includes math.cyr, prefixes all names with math_
  use math::sqrt;     → registers sqrt as alias for math_sqrt
  pub fn foo() {}     → exported (visible outside module)
  fn bar() {}         → private (module-internal only)

IMPLEMENTATION:

1. MODULE CONTEXT:
  Add current_module state (name offset in tok_names).
  When parsing mod name { ... } or including a module file:
    save old current_module
    set current_module = name
    parse contents
    restore current_module

2. NAME MANGLING:
  During PARSE_FN_DEF, if current_module != 0:
    actual_name = BUILD_METHOD_NAME(current_module, fn_name)
    # Reuses the same mangling as struct methods
  During FINDVAR/FINDFN, try both mangled and unmangled names.

3. VISIBILITY:
  Add pub_flags[256] — 1 bit per function/var (32 bytes).
  pub fn → set flag. fn → clear flag.
  When resolving names from outside current module:
    if (!pub_flags[fn_idx]) { error "private function" }

4. USE STATEMENTS:
  use_aliases[64] — maps alias name offset → mangled name offset
  When FINDFN fails on short name, check use_aliases for a mapping.

HEAP ADDITIONS (~3KB):
  current_module     [8]
  module_names[32]   [256]
  pub_flags[256]     [256]    (one byte per fn, packed later)
  use_aliases[64]    [1024]   (name_offset pairs)

MULTI-FILE: Once modules work within a single compilation, extend to:
  cat src/main.cyr src/math.cyr | cc2 > binary
  Each file auto-becomes a module based on filename.
  The preprocessor concatenates but tracks file boundaries.

PATTERN: Same as Kalyn compiler — file paths as module identifiers.
Avoids registry, package resolution, or complex name lookup.
'''

[[entries]]
name = "plan_iterators"
description = "Implementation strategy for for-in loops (syntactic sugar)"
category = "cyrius"
content = '''
DIFFICULTY: Low. Pure syntactic sugar in the parser. No codegen changes.

SYNTAX:
  for item in collection { body }

DESUGARING:
  Becomes:
    var _it = CollectionType_iter(collection);
    while (iter_has_next(_it) == 1) {
      var item = iter_next(_it);
      body
    }

PARSING:
  In PARSE_FOR, after consuming for:
    if next token is IDENT and token after that is IN (new keyword):
      # for-in loop
      parse item name
      consume IN
      parse collection expression
      desugar to while loop with iterator protocol

ITERATOR PROTOCOL (library convention):
  For each collection type, define:
    fn Vec_iter(v) → creates iterator state: { data_ptr, len, pos }
    fn iter_has_next(it) → load64(it + 16) < load64(it + 8)
    fn iter_next(it) → load value, increment pos

  The parser needs to know the collection type to call Vec_iter vs Map_iter.
  Option A: require explicit type annotation: for item in vec_iter(v) { }
  Option B: dispatch based on variable type (requires var_types tracking)

  Option A is simpler and avoids type system complexity.
  With methods: for item in v.iter() { body }
  This works TODAY with the method dispatch already implemented.

HEAP ADDITIONS: None. Just parser desugaring.

INTEGRATION: The for keyword (token 55) already exists. Add IN as token 72 or 73.
Alternatively, use the existing for loop syntax:
  for (var item = v.iter(); iter_has_next(item); iter_advance(item)) { }
This works NOW with no compiler changes — just library functions.
'''

[[entries]]
name = "plan_trait_impls"
description = "Implementation strategy for impl Trait for Type blocks"
category = "cyrius"
content = '''
DIFFICULTY: Medium. Builds on existing method dispatch.

SYNTAX:
  trait Display { fn format(self); }
  impl Display for Point { fn format(self) { ... } }

IMPLEMENTATION:
  trait Display { fn format(self); }
  → Registers trait name + method signatures in trait_table

  impl Display for Point { fn format(self) { ... } }
  → Parses as: fn Point_format(self) { ... }
  → Records that Point implements Display
  → Method dispatch via existing mangling: point.format() → Point_format(&point)

NAME RESOLUTION:
  When parser sees point.format():
    1. Look up Point_format (direct method) — found? call it.
    2. Not found? Search trait impls for any impl that defines format for Point.
    3. Still not found? Error.

HEAP ADDITIONS (~5KB):
  trait_names[32]          [256]  — trait name offsets
  trait_methods[32*8]      [2048] — method name offsets per trait
  trait_method_counts[32]  [256]  — methods per trait
  impl_records[64]         [1024] — {trait_idx, type_idx} pairs
  trait_count              [8]
  impl_count               [8]

TRAIT OBJECTS (dynamic dispatch):
  Already have vtable-based dispatch in trait.cyr library.
  Compiler-native traits would generate vtables automatically:
    fn Point_Display_vtable() { return &Point_format; }
  But this is a v1.0+ feature — start with static dispatch only.

PATTERN: trait.cyr already proves the concept with hand-written vtables.
Compiler-native impl blocks automate what the library does manually.
'''

[[entries]]
name = "plan_feature_flags"
description = "Implementation strategy for conditional compilation (#[cfg])"
category = "cyrius"
content = '''
DIFFICULTY: Low. Preprocessor-time decision. No codegen impact.

SYNTAX:
  #[cfg(feature = "debug")]
  fn debug_print(msg) { println(msg); }

  #[cfg(not(feature = "release"))]
  var VERBOSE = 1;

IMPLEMENTATION:
  Extend PREPROCESS to detect #[cfg(...)] patterns.
  Before lexing, scan for # followed by [ at beginning of line.

  1. Parse condition: feature = "name" | not(condition) | all(c1, c2) | any(c1, c2)
  2. Evaluate against enabled_features[] (set from command line)
  3. If false: skip tokens until the next top-level declaration (fn, var, struct, enum)
     or skip the entire { } block if the #[cfg] precedes one

COMMAND LINE:
  cc2 --feature debug --feature trace < src/main.cyr > binary
  Or via environment: CYRIUS_FEATURES="debug,trace" cc2 < main.cyr

HEAP ADDITIONS (~800 bytes):
  enabled_features[32]  [256]  — feature name offsets
  feature_count         [8]    — number of enabled features
  cfg_depth             [8]    — nesting depth for #[cfg] blocks

INTERACTION WITH PREPROCESSOR:
  #[cfg] is processed AFTER include expansion but BEFORE lexing.
  This means included files can have their own #[cfg] blocks.
  Order: PREPROCESS (expand includes) → CFG_FILTER (strip disabled blocks) → LEX

ALTERNATIVE (simpler):
  Use ifdef/endif preprocessor style:
    #ifdef DEBUG
    fn debug_print(msg) { println(msg); }
    #endif
  This is easier to parse (line-based) and matches C conventions.
'''

[[entries]]
name = "plan_string_type"
description = "Implementation strategy for owned String and borrowed &str types"
category = "cyrius"
content = '''
DIFFICULTY: Medium. Needs type tracking for automatic string operations.

DESIGN: String = heap pointer to { len: i64, data: bytes... }
  A String variable is a single i64 (pointer to the heap block).
  Length is stored at ptr-8 (before the data) or at ptr+0 with data at ptr+8.

OPTION A (simpler — length-prefixed heap block):
  String layout on heap:
    offset 0: length (i64)
    offset 8: byte data (length bytes, null-terminated for C compat)

  Variable holds pointer to offset 8 (the data).
  str_len(s) = load64(s - 8)   — O(1) length
  Null-terminated for compatibility with syscall(write) and C strings.

OPTION B (fat pointer — Rust/Go style):
  Variable holds TWO i64 values: {ptr, len}.
  Requires multi-word variable support.
  Not feasible in current everything-is-one-i64 model.

RECOMMENDED: Option A. Single i64 variable, length stored before data.

LIBRARY CHANGES:
  str_new(data, len) → alloc(len + 9); store64(ptr, len); memcpy(ptr+8, data, len); store8(ptr+8+len, 0); return ptr+8;
  str_len(s) → load64(s - 8)
  str_data(s) → s (pointer to bytes)
  str_cat(a, b) → new string with combined content
  str_eq(a, b) → compare by length then bytes

NOTE: This is what lib/str.cyr already does. The "String type" feature
is about making the compiler AWARE of strings (type tracking) so that
string literals automatically create String objects instead of raw pointers.

COMPILER CHANGE:
  When assigning a string literal to a variable:
    var msg = "hello";
  Currently: msg = pointer to data section (null-terminated, no length)
  With String type: msg = str_new("hello", 5) — heap-allocated with length

  This requires detecting string-typed variables in the parser.
  Simplest approach: treat ALL string literals as String objects.
  Breaking change for code that passes strings to syscall (expects raw pointer).
  Solution: keep raw string syntax as "hello" and add new syntax s"hello" for String.
'''

[[entries]]
name = "plan_generics_phase2"
description = "Implementation strategy for type-checked generics (no monomorphization)"
category = "cyrius"
content = '''
DIFFICULTY: Low for type checking. High for monomorphization.

KEY INSIGHT: Since everything is i64, Cyrius does NOT need monomorphization.
  Vec<i64> and Vec<*Point> generate identical code.
  Generics are a COMPILE-TIME check only — no runtime impact.

WHAT GENERICS PHASE 2 ADDS:
  1. Type parameter tracking: fn foo<T>(x: T) → T is a type variable
  2. Constraint checking: ensure T is used consistently within the function
  3. Error messages: "type mismatch: expected T=i64, got T=*Point"

IMPLEMENTATION:
  In PARSE_FN_DEF, after parsing <T>:
    Store T in generic_params[fn_idx] (new heap array)
    During function body parsing, track what T resolves to on first use
    On subsequent uses, verify consistency

  Example:
    fn swap<T>(a: *T, b: *T) { var tmp = *a; *a = *b; *b = tmp; }
    → T resolves to whatever the first call site provides
    → No code generation difference

HEAP ADDITIONS (~500 bytes):
  generic_params[256*4]  [1024] — up to 4 type params per function
  generic_bindings[256*4] [1024] — resolved types per param

WHAT THIS DOES NOT DO:
  - No monomorphization (Vec<i64> and Vec<Point> are the same binary code)
  - No specialization (no optimizing for specific types)
  - No trait bounds (impl Trait needed first)

THIS IS THE RIGHT APPROACH because:
  - Cyrius has no type system — adding one retroactively is massive
  - The safety benefit of generics is catching mistakes, not generating different code
  - Rust-style monomorphization requires a full type system + IR + code generation pipeline
  - Phase 2 generics are "documentation that the compiler verifies"
'''

# ================================================================
# IMPLEMENTED FEATURES — v0.9.7–v0.9.8
# ================================================================

[[entries]]
name = "pattern_matching"
description = "How match expressions work: syntax, codegen, scoped arms"
category = "cyrius"
content = '''
SYNTAX:
  match expr {
    0 => { stmts }
    42 => { stmts }
    _ => { stmts }     # default/wildcard
  }

TOKENS: MATCH=75. Arrow => is two tokens: EQ(4) + GT(20).

CODEGEN (same as switch):
  1. Evaluate match expr → store in hidden temp global var
  2. For each arm (except _):
     a. Load temp var, push
     b. Evaluate arm value, compare
     c. JCC(NE) to skip this arm
     d. SCOPE_PUSH, parse body, SCOPE_POP
     e. JMP to end (patched later)
     f. Patch the skip
  3. Default arm (_): just parse body, no comparison
  4. Patch all end jumps

WILDCARD DETECTION:
  Check if identifier is "_" (single underscore):
    load8(tok_names + noff) == 95 && load8(tok_names + noff + 1) == 0

SCOPING: Each arm gets its own scope (SCOPE_PUSH/POP).
Future: destructuring `Ok(v) => { use v }` requires enum constructors + block scoping.
'''

[[entries]]
name = "for_in_range"
description = "How for-in range loops work: for i in 0..10 { }"
category = "cyrius"
content = '''
SYNTAX:
  for i in start..end { body }

TOKENS: IN=76. Range (..) is two DOT(41) tokens.

DETECTION:
  In PARSE_FOR, before checking for ( (C-style for):
    if (PEEKT == IDENT) {
      save_ti; advance; if (PEEKT == IN) { ... for-in path ... }
      else { rewind to save_ti; fall through to C-style }
    }

DESUGARING (inline codegen, not AST transform):
  1. Evaluate start expr → store in new local var (iterator)
  2. Evaluate end expr → store in hidden global var (limit)
  3. Loop top: load iter, load limit, CMP, JCC(GE) to end
  4. SCOPE_PUSH, parse body, SCOPE_POP
  5. Load iter, add 1, store iter
  6. JMP to loop top
  7. Patch end jump

SCOPING: Iterator var and body share one scope (SCOPE_PUSH before start, POP after loop).
The iterator var is visible inside the body but not after the loop.

RANGE: Exclusive end (0..5 = 0,1,2,3,4). Matches Rust/Python convention.
'''

[[entries]]
name = "aarch64_syscall_portability"
description = "How portable syscall numbers work across x86 and aarch64"
category = "cyrius"
content = '''
PROBLEM:
  x86_64 and aarch64 have COMPLETELY DIFFERENT syscall numbers:
    read:  x86=0,  aarch64=63
    write: x86=1,  aarch64=64
    open:  x86=2,  aarch64=56
    close: x86=3,  aarch64=57
    brk:   x86=12, aarch64=214
    exit:  x86=60, aarch64=93

  Shared source files (src/frontend/*.cyr + src/backend/*.cyr) use raw syscall numbers.
  Cross-compiler runs on x86 but generates aarch64 code.

SOLUTION: SYS_* enum constants defined per-architecture.

  main.cyr (x86):
    enum Sys { SYS_READ=0; SYS_WRITE=1; SYS_OPEN=2; SYS_CLOSE=3; SYS_BRK=12; SYS_EXIT=60; }

  compiler_aarch64.cyr (cross-compiler, runs on x86):
    enum Sys { SYS_READ=0; SYS_WRITE=1; ... }  # x86 numbers (runs on x86!)

  For native aarch64 self-hosting (patched on ARM machine):
    enum Sys { SYS_READ=63; SYS_WRITE=64; SYS_BRK=214; SYS_EXIT=93; }

CROSS-COMPILER GOTCHA:
  The cross-compiler is an x86 binary. Its SYS_* values must be x86 numbers
  because THAT binary runs on x86. The code it GENERATES for aarch64 uses
  the raw numbers from the source being compiled.

  When self-hosting on aarch64, the SOURCE must have aarch64 numbers because
  the compiled binary runs on aarch64.

SYSCALL TRANSLATION LAYER (in aarch64 emitter):
  ESYSXLAT: chain of CMP X8,#N / B.NE +8 / MOVZ X8,#M
  Translates common x86 numbers to aarch64 at codegen time.
  Used by ESCPOPS for user-written syscall() calls.

ENCODING GOTCHA:
  MOVZ X8, #63 = 0xD28007E8 (not 0xD2800FE8!)
  Formula: 0xD2800000 | (imm16 << 5) | Rd
  63 << 5 = 0x7E0, NOT 0xFE0. Getting this wrong silently maps to wrong syscall.
'''

[[entries]]
name = "aarch64_escpops_fix"
description = "How the aarch64 syscall arg popping was fixed (pop-through-x0 approach)"
category = "cyrius"
content = '''
THE BUG:
  Original ESCPOPS popped args directly into target registers:
    EPOPRDI → x0 (arg1)
    EPOPRSI → x1 (arg2)
    ...
    EPOPR → x0 (syscall num, OVERWRITES arg1!)
  Then: mov x8, x0; shift x0←x1, x1←x2, etc. Lossy register shuffle.

THE FIX — pop-through-x0:
  Pop ALL values into x0, then MOV to target register:
    if (n >= 4) { EPOPR(S); EW(S, mov_x2_x0); }  # pop → x0, mov x2, x0
    if (n >= 3) { EPOPR(S); EW(S, mov_x1_x0); }  # pop → x0, mov x1, x0
    if (n >= 2) {
      EPOPR(S); EW(S, mov_x9_x0);  # pop arg1 → x0, save to x9
      EPOPR(S); EW(S, mov_x8_x0);  # pop syscall → x0, save to x8
      EW(S, mov_x0_x9);             # restore arg1 from x9
    }

WHY POP-THROUGH-x0 WORKS:
  - Only one register (x0) is used for all pops → no ordering ambiguity
  - Each pop immediately moves to the final target register
  - The syscall num/arg1 swap at the end uses x9 as a temporary
  - No shift chain, no lost values

LIFO ORDER:
  Push order: syscall_num, arg1, arg2, ..., argN
  Stack: [argN_top, ..., arg1, syscall_num_bottom]
  Pop order: argN first (top), syscall_num last (bottom)
  So the n>=N conditions fire from highest to lowest.

ARM64 ABI:
  Syscall: x8=number, x0-x5=args. svc #0.
  This matches the final register state after our pop sequence.
'''

[[entries]]
name = "module_system"
description = "How mod/use/pub work: namespace mangling, use aliases"
category = "cyrius"
content = '''
SYNTAX:
  mod math;                    # set current module to "math"
  pub fn add(a, b) { ... }    # public function in math module
  fn helper(x) { ... }        # private (no pub)

  mod main;                    # switch to main module
  use math.add;                # alias: add → math_add

IMPLEMENTATION:
  mod name; → sets GMOD(S) to the name offset in tok_names.
  When PARSE_FN_DEF sees GMOD != 0:
    Builds mangled name "module_function" in tok_names (at NPOS, permanent).
    Registers function under the mangled name.

  use modname.funcname; → builds "modname_funcname" in tok_names (at NPOS).
    Stores alias: use_from[uc] = funcname_offset, use_to[uc] = mangled_offset.

  FINDFN: After checking fn_names, checks use_aliases.
    If short name matches use_from[i], recursively calls FINDFN with use_to[i].

HEAP:
  current_module at 0x8FEC8 (8 bytes)
  pub_flags[256] at 0x8FED0 (256 bytes)
  use_from[64] at 0x8FFD0 (512 bytes)
  use_to[64] at 0x901D0 (512 bytes)
  use_count at 0x903D0 (8 bytes)

MANGLED NAME STORAGE GOTCHA:
  The mangled name must be stored PERMANENTLY in tok_names (at NPOS).
  Using a scratch area (offset 30000) causes names to be overwritten
  when multiple functions are defined — only the last one survives.
  Fix: append to tok_names at NPOS, same as the lexer does.

CROSS-MODULE CALLS:
  mod math; fn add(a, b) { ... }
  mod main; use math.add; var r = add(1, 2);
  → FINDFN("add") fails → checks aliases → finds "math_add" → calls it.
'''

[[entries]]
name = "operator_overloading"
description = "How operator overloading dispatches +,-,*,/ to Type_add/sub/mul/div"
category = "cyrius"
content = '''
DESIGN:
  When PARSE_EXPR sees + and the left operand has a struct type (GESTYPE > 0),
  dispatch to Type_add(lhs, rhs) instead of integer addition.

TRACKING:
  expr_stype (at S+0x903D8) — set in PARSE_FACTOR when loading a struct-typed var.
  Cleared to 0 for all other expressions (literals, function calls, etc.).
  Set from GVTYPE for globals, GLTYPE for locals (negative = struct ID).

DISPATCH (EMIT_OP_DISPATCH):
  1. Push lhs (already in rax from PARSE_TERM)
  2. Call PARSE_TERM to parse rhs, push result
  3. BUILD_OP_NAME: construct "TypeName_add" in tok_names (permanent, at NPOS)
  4. ECALLPOPS(2) + ECALLTO/ECALLFIX + ECALLCLEAN(2)
  5. Clear expr_stype to 0

BUILD_OP_NAME GOTCHA:
  Must use inline byte copy, NOT memcpy — the compiler source does not include
  lib/string.cyr. Using memcpy crashes the compiler during compilation.
  Fix: while loop with load8/store8.

TYPE ANNOTATION:
  var x: Str = str_from("hello");  → sets GLTYPE to Str struct ID (negative)
  Enables: x.len(), x + y (operator dispatch), x.contains("abc")
  Works for both global (GVTYPE/SVTYPE) and local (GLTYPE/SLTYPE) vars.
'''

[[entries]]
name = "block_body_closures"
description = "How block body closures work (and the global scope limitation)"
category = "cyrius"
content = '''
SYNTAX:
  var f = |x| { var y = x * 2; return y; };    # block body
  var g = |x| x * 2;                            # expression body (simpler)

BLOCK BODY:
  Parsed via PARSE_PROG inside the closure function.
  return statements emit JMP patches → patched to EFNEPI.
  Single EFNEPI at end handles both return paths and fall-through.

WORKS: Inside functions.
  fn make() {
    var f = |x| { if (x > 10) { return 42; } return 0; };
    return fncall1(f, 20);  // 42
  }

LIMITATION: Block body closures at GLOBAL scope produce wrong results.
  The closure compiles correctly but the interaction between the global
  context and the closure epilogue causes double-return issues.
  Fix: always use closures inside functions. This matches Rust convention.

IMPLEMENTATION:
  |params| { body } detected in PARSE_FACTOR when PEEKT == 13 (LBRACE).
  Same anonymous function generation as expression closures.
  PARSE_PROG parses body. Return patches collected via GRPC/SRPC.
  All patches resolved to single EFNEPI after body.
'''

[[entries]]
name = "fixup_table_expansion"
description = "Fixup table history: 0x8A000 (528) → 0x262000 (2048) → 0x2A2000 (8192)"
category = "cyrius"
content = '''
HISTORY:
  v1.4.0: Relocated from 0x8A000 (528 usable) to 0x262000 (2048 entries)
  v1.7.2: Relocated to 0x2A2000 to accommodate 512KB preprocessor buffer
  v1.7.5: Expanded to 4096 entries. fn_names/offsets/params relocated.
  v1.10.2: Expanded to 8192 entries (131072 bytes). fn_names/offsets/params
           relocated to 0x2C2000/0x2C4000/0x2C6000. brk at 0x2CE000.

CURRENT LAYOUT (v1.11.5):
  0x2A2000  fixup_tbl     [131072] 8192 entries × 16 bytes
  0x2C2000  fn_names      [8192]   1024 function name offsets
  0x2C4000  fn_offsets    [8192]   1024 function code offsets
  0x2C6000  fn_params     [8192]   1024 function param counts
  0x2C8000  fn_body_start [8192]   1024 inline body start token indices
  0x2CA000  fn_body_end   [8192]   1024 inline body end token indices
  0x2CC000  fn_inline     [8192]   1024 inline eligibility flags
  0x2CE000  brk

FIXUP ENTRY FORMAT (16 bytes each):
  [0..7]  code offset — position in codebuf where the address goes
  [8..15] type (high byte) | index (low 7 bytes)
    type 0 = variable address (dbase + cumulative var offsets)
    type 1 = string address (dbase + totvar + string_offset)
    type 2 = function call (relative offset for CALL instruction)
    type 3 = absolute function address (for function pointers)
    type 4 = tail call (aarch64 B instruction, not BL)

OVERFLOW CHECK:
  All fixup writers check fc >= 8192: RECFIX, ESADDR, ECALLFIX,
  ETAILJMP, &fn handler, closure handler.

SIZING GUIDE:
  Each function call, string literal, global var reference, and function
  pointer generates one fixup entry. argonaut (358 functions, 6257 lines)
  uses ~1100 entries. 8192 limit supports programs well past 20K lines.
'''

[[entries]]
name = "str_data_buffer_expansion"
description = "String literal buffer relocated from 0x69000 to 0x61000 for 32KB capacity"
category = "cyrius"
content = '''
PROBLEM:
  String literals are stored at compile time in str_data buffer.
  Originally at 0x69000 with str_pos at 0x69800 — only 2048 bytes
  of usable space before str_pos was overwritten.

  Programs with >2KB of string literals (common with multiple includes
  and println calls) would overflow into str_pos and data_size,
  corrupting the ELF data section layout.

DIAGNOSIS:
  Counted total string literal bytes across all includes + program:
    grep -oP '"[^"]*"' | awk '{ total += length($0) - 1 }'
  Result: 4196 bytes vs 2048 limit.

FIX (v1.4.0):
  1. Relocated str_data from 0x69000 to 0x61000 (32KB free space between
     var_sizes at 0x60800 and old str_data at 0x69000)
  2. str_pos stays at 0x69800, data_size at 0x69808 (unchanged)
  3. Updated all 0x69000 references in: lex.cyr, fixup.cyr, aarch64/fixup.cyr
  4. Two-step bootstrap required

HEAP LAYOUT (data region):
  0x60000  var_noffs  [2048]   256 variable name offsets
  0x60800  var_sizes  [2048]   256 variable sizes
  0x61000  str_data   [32768]  String literal bytes (was 0x69000, 2KB)
  0x69800  str_pos    [8]      Current write position in str_data
  0x69808  data_size  [8]      Total variable data bytes for ELF
'''

[[entries]]
name = "shared_library_output"
description = "shared; directive emits ET_DYN ELF instead of ET_EXEC for .so output"
category = "cyrius"
content = '''
FEATURE (v1.1.0):
  The `shared;` keyword at top-level sets kernel_mode=2, which causes
  the ELF emitter to produce an ET_DYN (type 3) binary instead of
  ET_EXEC (type 2). This makes the output recognizable as a shared
  library by tools like `file`, `nm`, and `readelf`.

USAGE:
  shared;
  pub fn add(a, b) { return a + b; }
  pub fn mul(a, b) { return a * b; }

  Compile: cat mylib.cyr | cc2 > mylib.so

IMPLEMENTATION:
  1. Lexer: `shared` keyword (token 78) recognized in LEXID
  2. Parser: top-level `shared;` sets kernel_mode at 0x8FCA0 to 2
  3. Fixup: EMITELF checks kernel_mode:
     - 0 = ET_EXEC (normal binary, base 0x400000)
     - 1 = kernel (ELF32 multiboot, base 0x100000)
     - 2 = ET_DYN (shared library)
  4. EMITELF_USER(S, etype) handles both ET_EXEC and ET_DYN —
     only the e_type field in the ELF header differs

LIMITATIONS:
  - No PIC (position-independent code) — addresses are absolute
  - No symbol table or dynamic section — nm can't resolve names
  - No PLT/GOT — dlsym won't find functions
  - Useful as proof-of-concept; full .so requires PIC codegen (future)
'''

[[entries]]
name = "address_based_operator_overloading"
description = "Multi-field structs pass addresses (not values) to operator functions"
category = "cyrius"
content = '''
FEATURE (v1.2.0):
  Operator overloading (`a + b` → `Type_add(a, b)`) needs to pass struct
  data to the operator function. For single-field structs (8 bytes), the
  value fits in a register. For multi-field structs, only the address fits.

DISPATCH RULE:
  The compiler checks var_sizes[var_index]:
  - size == 8  → pass VALUE (load64 from variable, put in register)
  - size > 8   → pass ADDRESS (&var, pointer to first field)

  This means operator functions for multi-field structs receive pointers:

  struct Vec3 { x; y; z; }  # 24 bytes → pass by address

  fn Vec3_add(a, b) {
    var rx = load64(a) + load64(b);
    var ry = load64(a + 8) + load64(b + 8);
    var rz = load64(a + 16) + load64(b + 16);
    # Must return a new struct (allocate or use result var)
    store64(a, rx); store64(a + 8, ry); store64(a + 16, rz);
    return a;
  }

  var a = Vec3 { 1, 2, 3 };
  var b = Vec3 { 10, 20, 30 };
  var c = a + b;  # calls Vec3_add(&a, &b)

IMPLEMENTATION:
  In EMIT_OP_DISPATCH (parse.cyr):
  1. GESVAR(S) retrieves the variable index of the left operand
  2. var_sizes[idx] checked: if >8, emit LEA (load effective address)
     instead of MOV (load value)
  3. Same check for right operand
  4. Call Type_op(left, right) with addresses or values accordingly

BACKWARD COMPATIBLE:
  Single-field structs and type-annotated scalars continue to pass values.
  Only structs with 2+ fields (>8 bytes) switch to address passing.
'''

[[entries]]
name = "emitelf_dedup"
description = "EMITELF and EMITELF_SHARED merged into EMITELF_USER(S, etype) — 2KB saved"
category = "cyrius"
content = '''
REFACTOR (v1.5.0):
  EMITELF and EMITELF_SHARED were 95% identical — both emit an x86_64
  ELF binary with the same header layout, program header, code section,
  variable data, and string data. The only difference: e_type field
  (byte 16 of ELF header) is 2 (ET_EXEC) vs 3 (ET_DYN).

BEFORE:
  fn EMITELF(S) { ... 60 lines ... }      # e_type = 0x0002
  fn EMITELF_SHARED(S) { ... 40 lines ... } # e_type = 0x0003

AFTER:
  fn EMITELF_USER(S, etype) {
    ...
    # e_type from parameter:
    S64(O + op, 0x00000001003E0000 | etype);
    ...
  }

  fn EMITELF(S) {
    if (kernel_mode == 1) { EMITELF_KERNEL(S); return 0; }
    if (kernel_mode == 2) { EMITELF_USER(S, 3); return 0; }
    EMITELF_USER(S, 2);
    return 0;
  }

RESULT: Compiler shrunk 138KB → 136KB (2KB saved). Self-hosting verified.

KEY INSIGHT:
  The ELF e_type field is at the lowest 16 bits of the third qword
  (offset 16). Using `0x00000001003E0000 | etype` constructs:
    e_type = etype (2 or 3)
    e_machine = 0x3E (x86_64)
    e_version = 1
  All in one 64-bit store.
'''

[[entries]]
name = "multi_pass_preprocessor"
description = "Multi-pass preprocessor: include inside #ifdef in included files (v1.6.5)"
category = "cyrius"
content = '''
PROBLEM:
  Preprocessor made a single pass. When expanding include "file.cyr",
  the file content was read into the output buffer but never preprocessed.
  So #ifdef and nested include inside included files were ignored.

FIX:
  Split PREPROCESS into PP_PASS (one pass) and PREPROCESS (loop).
  PP_PASS returns 1 if any include was expanded, 0 if none.
  PREPROCESS runs PP_PASS in a loop until no includes found, max 16 passes.

  fn PP_PASS(S) {
    ... (same as old PREPROCESS but tracks had_include flag)
    return had_include;
  }

  fn PREPROCESS(S) {
    var pass = 0;
    var go = 1;
    while (go == 1) {
      var had = PP_PASS(S);
      pass = pass + 1;
      if (had == 0) { go = 0; }
      if (pass >= 16) { go = 0; }
    }
    return 0;
  }

  Pass 1: expands top-level includes, evaluates top-level #define/#ifdef
  Pass 2: newly-included content gets its #ifdef/include processed
  Pass 3+: handles deeper nesting (rare, usually 2 passes suffice)

WHY 16: Arbitrary safety limit. Real-world code rarely nests beyond 3 levels.
The compiler itself (7 includes, no nesting) completes in 2 passes.

PERFORMANCE: Each pass copies the full source. For the compiler (136KB source
after expansion), each pass takes <1ms. 2 passes = no measurable impact.
'''

[[entries]]
name = "arch_specific_asm_mnemonics"
description = "Moving ASM_MNEMONIC from shared parse.cyr to arch-specific emit files (v1.6.5)"
category = "cyrius"
content = '''
PROBLEM:
  ASM_MNEMONIC was defined in parse.cyr (shared code). It contained x86 encodings
  (cli=0xFA, sti=0xFB, etc). The aarch64 compiler included the same parse.cyr,
  so x86 asm { cli; } silently emitted 0xFA (invalid aarch64 instruction).
  No error, just wrong code.

FIX:
  Moved ASM_MNEMONIC and all ASM_* helpers from parse.cyr to emit files:
  - src/backend/x86/emit.cyr: x86 mnemonics (cli, sti, hlt, nop, iretq, wrmsr, rdmsr, ...)
  - src/arch/aarch64/emit.cyr: aarch64 mnemonics (svc, wfi, wfe, dmb, dsb, isb, nop, ret)

  parse.cyr still calls ASM_MNEMONIC(S) — the function is resolved from whichever
  emit.cyr was included. Since emit.cyr is included BEFORE parse.cyr, the function
  is registered in pass 1 and available when parse.cyr calls it in pass 2.

  Unknown mnemonics now error: "error:N: unknown aarch64 asm mnemonic"

KEY INSIGHT: Function override does NOT work for indirect calls in Cyrius.
  If fn A() calls fn B(), and B() is redefined later, A still calls the original B
  (because the BL fixup was already resolved). So overriding ASM_MNEMONIC in
  fixup.cyr (after parse.cyr) does NOT work. The function must be in a file
  included BEFORE parse.cyr (emit.cyr).
'''

[[entries]]
name = "aarch64_kernel_elf"
description = "aarch64 kernel ELF emission: base address, SP preamble, fixup (v1.6.5)"
category = "cyrius"
content = '''
AARCH64 KERNEL ELF:
  Base: 0x40000000 (standard ARM kernel load address for QEMU -kernel)
  Entry: 0x40000078 (base + 64 byte ELF header + 56 byte program header)
  Format: ELF64, e_machine=0xB7 (AArch64), statically linked
  No multiboot (ARM uses device tree, not BIOS/multiboot)

SP PREAMBLE (16 bytes, 4 instructions):
  Emitted between ELF headers and compiler-generated code.
  Sets stack pointer to top of memsz region (base + filesz + 64KB).

  MOVZ x9, #stack_lo
  MOVK x9, #stack_mid, LSL#16
  MOVK x9, #stack_hi, LSL#32
  MOV SP, x9   (encoded as ADD SP, x9, #0 = 0x9100013F)

  Without this, first function prologue (stp x29,x30,[sp,#-16]!) faults
  because SP=0 at kernel entry.

FIXUP ADJUSTMENT:
  entry = 0x40000078  (was 0x100060 from x86 — wrong)
  dbase = entry + preamble(16) + aligned_code_size
  String addresses: dbase + total_var_bytes + string_offset
  Function calls: BL relative (codebuf-internal, no preamble adjustment needed)

BOOT COMMAND:
  qemu-system-aarch64 -M virt -cpu cortex-a57 -kernel build/agnos_aarch64 -serial stdio -display none
'''

[[entries]]
name = "packed_result_implementation"
description = "Packed Result type: zero-alloc Ok/Err using bit 63 (v1.5.3, agnosys)"
category = "cyrius"
content = '''
ENCODING:
  Ok(value):  return value directly (bit 63 = 0 for all valid pointers and positive ints)
  Err(ptr):   return ptr | 0x8000000000000000 (bit 63 = 1)
  is_ok(r):   r >= 0 (signed comparison — bit 63 test)
  is_err(r):  r < 0
  payload(r): r & 0x7FFFFFFFFFFFFFFF (mask off sign bit)

WHY IT WORKS:
  x86_64 user pointers are canonical: always < 2^47 (bit 63 = 0).
  Heap pointers from alloc() are always positive.
  Syscall return values (pids, fds) are always positive on success.
  So bit 63 is always 0 for valid Ok values.

PACKED ERRORS (dual encoding):
  For errors that don't need a message string:
    syserr_pack(kind, errno) = kind << 16 | errno
  For errors with messages:
    syserr_new(kind, errno, message) = alloc(24) → heap pointer

  Dispatch in syserr_kind/errno/message:
    if (e < 0x100000) → packed (kind = e >> 16, errno = e & 0xFFFF)
    else → heap pointer (kind = load64(e), errno = load64(e+8))

PERFORMANCE (agnosys benchmarks):
  Ok(42):         15ns → 2ns (7.5x faster — no alloc)
  err_from_errno: 38ns → 21ns (45% faster — packed, no alloc on hot path)
  query_sysinfo:  1110ns → 664ns (40% faster — caller buffer, no memset)
  getpid/getuid:  parity with Rust

BACKWARD COMPATIBILITY:
  The API (Ok, Err, is_ok, is_err_result, payload) is identical.
  Old heap-based tagged.cyr still works. Projects choose which to use
  by vendoring the appropriate lib/tagged.cyr.
'''

[[entries]]
name = "emovi_optimization"
description = "EMOVI code size optimization: xor eax,eax for 0, mov eax,imm32 for small values (v1.6.7)"
category = "cyrius"
content = '''
BEFORE: EMOVI always emitted movabs rax, imm64 (10 bytes: 48 B8 + 8 bytes).
AFTER:
  v == 0:              xor eax, eax (2 bytes: 31 C0) — clears rax, faster
  0 < v < 0x80000000:  mov eax, imm32 (5 bytes: B8 + 4 bytes) — zero-extends to rax
  else:                movabs rax, imm64 (10 bytes) — unchanged

  fn EMOVI(S, v) {
    if (v == 0) { EB(S, 0x31); EB(S, 0xC0); return 0; }
    if (v > 0) { if (v < 0x80000000) {
      EB(S, 0xB8);
      EB(S, v & 0xFF); EB(S, (v >> 8) & 0xFF);
      EB(S, (v >> 16) & 0xFF); EB(S, (v >> 24) & 0xFF);
      return 0;
    } }
    E2(S, 0xB848); E8(S, v); return 0;
  }

IMPACT: Compiler 136KB → 131KB (5KB saved). All generated binaries shrink.
Requires two-step bootstrap: old cc2 → cc3 (larger), cc3 → cc4 (optimized), cc4 == cc5.
'''

[[entries]]
name = "preprocessor_macros_implementation"
description = "Preprocessor macros with args: define, store, expand (v1.6.7)"
category = "cyrius"
content = '''
SYNTAX: #define NAME(p1, p2) body text
INVOKE: NAME(arg1, arg2)

STORAGE (in PP_PASS):
  When #define with ( is detected:
  - Hash name (stopping at paren) → S+0x92000 (16 name hashes)
  - Copy entire line after #define to S+0x93000 (16KB text storage)
  - Store text offset at S+0x92080 and length at S+0x92100
  - Increment _pp_macro_count, advance ip via PP_SKIPLINE

EXPANSION (in PP_MACRO_PASS, separate pass after all includes resolved):
  - Scan input for uppercase identifiers matching a macro name hash
  - Verify ( follows the identifier
  - Parse params from stored definition text
  - Parse args from input (comma-separated, handles nested parens)
  - Walk body text, replace param names with arg text (word-boundary check)
  - Write expanded text + newline to output

KEY DESIGN: macros are stored during the include/ifdef pass but expanded in a
SEPARATE pass afterward. This avoids the bug where PP_STORE_MACRO consumed
too much input (function call boundary issue with the Cyrius compiler).

LIMITATIONS:
  - Macro names must start uppercase (A-Z) to avoid false matches
  - Up to 16 macros
  - Single-line body only (no multi-line macros)
  - No nested macro expansion
'''

[[entries]]
name = "return_comparison_fix"
description = "return expr == expr now works: PARSE_RETURN uses PARSE_CMP_EXPR (v1.7.0)"
category = "cyrius"
content = '''
BUG: return a == b; failed with "expected ';', got '=='"
ROOT CAUSE: PARSE_RETURN called PARSE_EXPR which stops at comparison operators.
FIX: PARSE_RETURN now calls PARSE_CMP_EXPR which handles ==, !=, <, >, <=, >=.

  fn PARSE_RETURN(S) {
    STI(S, GTI(S) + 1);
    PARSE_CMP_EXPR(S);   # was PARSE_EXPR(S)
    ...
  }

Comparisons already worked in if() conditions (via ECONDCMP) and in function
arguments (via PARSE_CMP_EXPR). Only return statements were missing.
'''

[[entries]]
name = "input_buffer_256kb"
description = "Input buffer expanded to 256KB for large programs with many includes (v1.7.0)"
category = "cyrius"
content = '''
PROBLEM: Preprocessor expands includes into output buffer (S+0x222000, 262144 bytes),
then copies back to input buffer (S+0, 131072 bytes). Large programs with many
includes (e.g., compiler itself at 152KB after expansion) overflowed silently,
producing confusing parse errors or segfaults.

FIX: Allow copy-back up to 262144 bytes. The overflow into codebuf (S+0x20000) is
safe because the lexer consumes the entire source into token arrays BEFORE code
generation starts. By the time codegen writes to codebuf, the source text is no
longer needed at S+0.

LIMITS:
  Input (before preprocessing): 131072 bytes (read from stdin)
  Expanded source (after preprocessing): up to 262144 bytes
  Codebuf: 262144 bytes (256KB) (with overflow check in EB)
  Preprocess output buffer: 262144 bytes

CODEBUF OVERFLOW: EB() now checks cp >= 262144 and errors:
  "error: codebuf overflow (N/262144 bytes) — program too large"
'''

[[entries]]
name = "dead_code_elimination"
description = "Dead code elimination: token scan + 3-byte stubs for unreachable functions (v1.7.0)"
category = "cyrius"
content = '''
APPROACH: After pass 1 (all names registered), before pass 2 (emit bodies):
  - Scan all tokens for IDENT references (skip those preceded by FN keyword)
  - Compare each IDENT against function name using STREQ (string comparison)
  - Skip DCE for module-scoped functions (GMOD != 0)
  - Skip DCE for names containing underscore (likely mangled: Type_method)

PASS 2 CHECK: For each top-level fn definition:
  if (not in module AND no underscore AND name not found in token scan):
    emit stub instead of full body

STUB (3 bytes):
  S64(S + 0x26C000 + fi * 8, GCP(S));  # set fn_offsets to stub location
  EB(S, 0x31); EB(S, 0xC0);            # xor eax, eax
  EB(S, 0xC3);                          # ret
  # Skip function body tokens (brace counting)

WHY STUBS NOT SKIP: Skipping entirely leaves fn_offsets[idx] = -1.
  Fixup type 2 computes: target(-1) - coff - 4 → wild jump → segfault.
  Fixup type 3 computes: entry + (-1) → invalid address → segfault.
  Stubs give fixups a valid 3-byte target that safely returns 0.

KEY BUG FIXED: Initial implementation compared TOKVAL offsets (tok_names buffer
  positions). Different tokens for the same identifier have different offsets
  because the lexer stores each occurrence separately. Fix: use STREQ to compare
  the actual string content at each offset.

PERFORMANCE: O(F * T) where F = functions, T = tokens. For the compiler
  (248 functions, ~20K tokens), the scan adds ~1ms. Negligible.

SAVINGS: ~1.5KB on hello-world with 8 stdlib includes (unused stdlib functions
  replaced with 3-byte stubs). Larger programs with more unused library code
  see proportionally more savings.
'''

[[entries]]
name = "vcnt_expansion_and_overlap_fix"
description = "VCNT expanded to 2048, fixed var_noffs/var_sizes overlap bug (v1.7.0)"
category = "cyrius"
content = '''
BUG: var_noffs at 0x60000 [4096 bytes = 512 entries] and var_sizes at 0x60800
  [4096 bytes = 512 entries] overlapped when VCNT > 256:
    var_noffs[256] writes to 0x60000 + 256*8 = 0x60800 → overwrites var_sizes[0]
  This was introduced in v1.5.2 when expanding from 256 to 512 entries.
  Worked by luck because most programs had < 256 variables.

FIX: Relocated to non-overlapping regions with 2048 entries each:
    0x60000  var_noffs [16384]  2048 entries (16KB)
    0x64000  var_sizes [16384]  2048 entries (16KB)
    0x68000  str_data  [8192]   string literals (8KB)

IMPACT: agnostik port (12 modules, ~500 variables + ~200 enum variants)
  now fits comfortably. Previous 512 limit hit at ~60% through modules.

VCNT STILL NEVER RESETS: Each function's locals, params, and arrays permanently
  consume slots. With 2048 slots, practical programs can have ~1500 variables
  + ~500 enum variants. Full fix (stack-local allocation, enum constants without
  VCNT) deferred.
'''

[[entries]]
name = "constant_folding"
description = "Constant folding: codegen-level NUM OP NUM → single EMOVI (v1.7.3)"
category = "cyrius"
content = '''
MECHANISM:
  PARSE_FACTOR sets _cfp (codebuf position before EMOVI) and _cfo (flag) for
  small positive literals (0 < v < 0x80000000 → 5-byte mov eax, imm32).

  PARSE_TERM checks: if _cfo == 1 AND next token is NUM AND result is also
  small positive, rewind SCP to _cfp and emit EMOVI(folded_result).
  The rewind replaces exactly 5 bytes with 5 bytes — no code corruption.

OPERATORS: * / << >> (high-precedence, in PARSE_TERM while loop).
  Low-precedence (+, -, &, |, ^) NOT folded (would require PARSE_EXPR changes).

CRITICAL BUG (found during 4 failed attempts):
  _cfo flag leaked from inside parenthesized subexpressions.
  (n - 6) * 8 would fold 6 * 8 = 48 instead of (n-6) * 8.
  The 6 inside (n - 6) set _cfo = 1, which persisted past the closing ).
  FIX: clear _cfo after evaluating parenthesized expressions in PARSE_FACTOR.

SAFETY CONSTRAINTS:
  - Both operands must be small positive (0 < v < 0x80000000)
  - Result must also be small positive (fits in 5-byte EMOVI)
  - Larger constants (like 1 << 56 = 10-byte movabs) are correctly skipped
  - This prevents SCP rewind from overwriting subsequent code

SELF-HOSTING: cc3 == cc4 == cc5 == cc6 (all 140KB, identical binaries).
  The compiler source has no NUM * NUM patterns, so no folding occurs during
  self-compilation. User programs benefit from the optimization.
'''

[[entries]]
name = "tail_call_optimization"
description = "Tail call optimization: return fn(args) → epilogue + jmp (v1.7.2)"
category = "cyrius"
content = '''
DETECTION (in PARSE_RETURN):
  1. Check GINFN(S) == 1 (inside a function)
  2. Check PEEKT(S) == IDENT and next token is LPAREN
  3. Scan forward to matching RPAREN, verify next token is SEMI
  4. If all checks pass: this is a tail call

EMIT:
  1. Parse args normally (PARSE_CMP_EXPR for each, push to stack)
  2. ECALLPOPS — pop args into registers (rdi, rsi, rdx, rcx, r8, r9)
  3. ETAILJMP — emit epilogue (mov rsp, rbp; pop rbp) + jmp rel32 with fixup

  fn ETAILJMP(S, fnidx) {
    EB(S, 0xC9);       # leave (mov rsp,rbp; pop rbp)
    # pop r12 (41 5C)
    EB(S, 0x41); EB(S, 0x5C);
    EB(S, 0x5B);       # pop rbx
    EB(S, 0xE9);       # jmp rel32
    ... fixup entry (type 2) ...
  }

KEY VALIDATION: step 3 checks ) is followed by ; to avoid folding
  return fn() + expr; (which is NOT a tail call).

PERFORMANCE: 1 million recursive calls without stack overflow.
  fn count(n) { if (n == 0) { return 42; } return count(n - 1); }
  count(1000000) returns 42 (would segfault without TCO).
'''

[[entries]]
name = "f64_transcendentals"
description = "x87 FPU transcendental builtins: sin, cos, exp, ln, log2, exp2 (v1.7.8)"
category = "cyrius"
content = '''
BUILTINS (20 total):
  SSE2:  f64_add, f64_sub, f64_mul, f64_div (addsd/subsd/mulsd/divsd)
         f64_from (cvtsi2sd), f64_to (cvttsd2si)
         f64_eq/f64_lt/f64_gt (ucomisd + setcc)
         f64_neg (xorpd 0; subsd), f64_sqrt (sqrtsd)
  SSE4.1: f64_floor (roundsd mode 1), f64_ceil (roundsd mode 2)
  Integer: f64_abs (AND 0x7FFFFFFFFFFFFFFF — clear sign bit)
  x87 FPU: f64_sin (fsin), f64_cos (fcos)
           f64_ln (fldln2; fxch; fyl2x)
           f64_log2 (fld1; fxch; fyl2x)
           f64_exp (fldl2e; fmulp; fld st0; frndint; fsub; fxch; f2xm1; fld1; faddp; fscale; fstp st1)
           f64_exp2 (same as exp without fldl2e multiply)

x87 BRIDGE:
  SSE2 value is in rax (f64 bit pattern). x87 uses its own FPU stack.
  Transfer: sub rsp,16; mov [rsp],rax; fld qword [rsp] → compute → fstp qword [rsp]; mov rax,[rsp]; add rsp,16
  Emit helpers: EX87PUSH(S) and EX87POP(S) in emit.cyr.

USAGE:
  All f64 builtins take and return i64 (f64 bit pattern). Values are NOT floats
  in the Cyrius type system — they are integers that happen to hold IEEE 754 bits.
  var pi = 0x400921FB54442D18;   # pi as f64 bits
  var s = f64_sin(pi);           # sin(pi) ≈ 0
  var n = f64_to(s);             # convert to integer: 0
  var x = f64_from(42);          # 42.0 as f64 bits
  var r = f64_sqrt(x);           # sqrt(42.0)

TOKEN ASSIGNMENTS:
  62=f64_add 63=f64_sub 64=f64_mul 65=f64_div 66=f64_from 67=f64_to
  68=f64_eq 69=f64_lt 70=f64_gt 71=f64_neg
  79=f64_sqrt 80=f64_abs 81=f64_floor 82=f64_ceil
  83=f64_sin 84=f64_cos 85=f64_exp 86=f64_ln 87=f64_log2 88=f64_exp2
  89=f64v_add 90=f64v_sub 91=f64v_mul (SIMD batch, SSE2 packed)
  92=f64_round (SSE4.1 round to nearest)
  93=f64v_div (divpd) 94=f64v_sqrt (sqrtpd)
  95=f64v_abs (andpd+mask) 96=f64v_fmadd (mulpd+addpd)
  97=ret2 (return two values rax:rdx) 98=rethi (read rdx from last call)

STDLIB ADDITIONS (v1.9.4):
  fmt_float(val, decimals) — format f64 as "3.141593". Uses f64_floor + f64_round.
  fmt_float_buf(val, buf, decimals) — write to buffer, return length.
  getenv(name) — read env var from /proc/self/environ, heap-allocated result.
'''

[[entries]]
name = "identifier_deduplication"
description = "LEXID deduplicates identifiers in tok_names to reduce buffer usage ~50% (v1.7.8)"
category = "cyrius"
content = '''
PROBLEM:
  Every identifier occurrence stored a NEW copy in tok_names (0x50000, 64KB).
  The compiler source itself used ~65500 bytes — at the buffer limit.
  Adding any new feature (functions, variables) would overflow.

FIX:
  After writing a new identifier to tok_names, LEXID scans backwards through
  all previously stored identifiers. If an identical string is found, the new
  entry is discarded (NPOS rewound) and the token value reuses the existing offset.

  Key: reuses existing LEXID local variables (bl, go, c) to avoid adding new
  unique identifiers to tok_names (chicken-and-egg: the dedup code itself must
  fit within the old limit to compile).

BUGS FOUND DURING IMPLEMENTATION:
  1. Variable scope: 'c' declared inside while{if{}} was at scope depth 3,
     invisible to dedup code at scope depth 1. Fix: declare 'c' at function level.
  2. Return value clobber: reusing 'p' (LEXID's position parameter) as a loop
     counter set p=0, causing the main LEX loop to restart from position 0
     (infinite token generation). Fix: use 'dc' (declared inside dedup).
  3. klen mismatch: after dedup changes 'ws' to a distant offset, klen = npos - ws
     was a huge number (e.g., 60000). All keywords misidentified as IDENT.
     Fix: compute klen BEFORE dedup modifies ws.

BOOTSTRAP:
  Required two-step bootstrap because the dedup code added ~30 bytes of identifier
  occurrences, pushing past the 65500 limit:
  1. Rename PARSE_CMP_EXPR → PCMPE (saves 90 bytes) → compiles with old cc2
  2. Build dedup-enabled compiler → now has ~30000 bytes headroom
  3. Future changes have plenty of room

RESULT: tok_names usage dropped from ~65500 to ~30000 bytes for the compiler source.
'''

[[entries]]
name = "jump_table_switch"
description = "Dense switch statements use O(1) jump table instead of O(n) compare chain (v1.7.7)"
category = "cyrius"
content = '''
HEURISTIC: Pre-scan case values. If case_count >= 4 AND (max - min + 1) <= 2 * count
  AND range < 256 → use jump table. Otherwise use compare-and-branch chain.

CODEGEN (x86_64):
  # Setup: bounds check
  push rax; mov rax, min; mov rcx, rax; pop rax   # rcx = min
  sub rax, rcx                                      # rax = value - min (0-based)
  push rax; mov rax, range; mov rcx, rax; pop rax  # rcx = range
  cmp rax, rcx                                      # bounds check
  ja default                                        # out of range

  # Table lookup
  lea rcx, [rip + table_offset]   # table base
  movsxd rax, [rcx + rax*4]       # load 4-byte signed offset
  add rax, rcx                     # absolute target
  jmp rax                          # dispatch

  # Table: (range+1) entries × 4 bytes, offsets from table base
  # Patched as case bodies are parsed. Unmatched slots → default/end.

TWO-PASS WITHIN SWITCH:
  Pass 1: scan tokens to collect case values (min/max/count). No code emitted.
  Pass 2: emit jump table or chain based on heuristic.
  Token index saved/restored between passes.

CASE BODY PATCHING:
  Table entries start as 0. As each case body is parsed, its code offset is
  written to table[val - min] as a 4-byte offset from table base. After all
  cases, unpatched (0) entries are filled with the end/default offset.
'''

[[entries]]
name = "inline_small_functions_design"
description = "HISTORICAL: Token replay inlining design — disabled v1.11.3 due to I-cache regression"
category = "cyrius"
content = '''
STATUS: DISABLED (v1.11.3, _INLINE_OK = 0). Design preserved for future re-enablement.

DESIGN:
  Small functions (1 parameter, <= 6 body tokens) are inlined at the call site
  by replaying their body tokens in the caller's parsing context, with the
  parameter name mapped to the actual argument expression.

METADATA TABLES (heap 0x2C8000-0x2D2000):
  fn_body_start[1024] at 0x2C8000 — token index of body start per function
  fn_body_end[1024] at 0x2CA000   — token index of body end per function
  fn_inline[1024] at 0x2CC000     — 1 if function is eligible for inlining
  fn_pnames[1024] at 0x2CE000     — param name offset per function (1-param only)
  brk moved from 0x2C8000 to 0x2D2000.

ELIGIBILITY:
  During pass 2 (PARSE_FN_DEF), after parsing a function body:
  - Check param count == 1
  - Check body token count <= 6
  - If eligible: set SFINL(fi, 1), record body start/end/pname

INLINING (PARSE_FNCALL):
  When calling a function where GFINL(fi) == 1:
  1. Evaluate the single argument expression (already in rax)
  2. Save current token index
  3. Set TI to fn_body_start[fi]
  4. Replay body tokens with parameter name remapping:
     when the parser encounters the param name, it reads from the argument
     location instead of a local variable slot
  5. Restore TI after replay

DEPTH LIMIT:
  Max inline depth 3. The GIDEP(S) counter tracks current nesting.
  If depth >= 3, fall back to normal function call.
  Tail call optimization is disabled inside inline replay (checked via GIDEP > 0).

AARCH64:
  Same token replay mechanism — inlining is arch-independent (frontend only).
  The replayed tokens go through normal parse/emit which uses the active backend.
'''

[[entries]]
name = "r12_spill_register_allocation_reverted"
description = "HISTORICAL: R12 spill added v1.10.0, REVERTED v1.11.2 — 19-125% benchmark regressions"
category = "cyrius"
content = '''
STATUS: REVERTED (v1.11.2). Push rbx + push r12 added 7 bytes and 4 stack ops
per function. Benchmarks showed 19-125% regressions on heap alloc, syscalls, I/O.
ESPILL/EUNSPILL are now aliases for push rax / pop rax. Prologue reverted to
push rbp; mov rbp, rsp. Stack param offset back to +16.

PROBLEM (that motivated this, still unsolved):
  Expression evaluation uses push/pop to save intermediate values:
    a + b * c → eval a, push rax, eval b*c, pop rcx, add
  Each push/pop is 1+1 bytes but requires memory round-trip (stack store/load).

FIX — R12 SPILL:
  ESPILL/EUNSPILL replace EPUSHR/EPOPR in PARSE_TERM and PEXPR.
  First nesting level uses callee-saved R12 (register-register, zero latency).
  Deeper levels fall back to push/pop (unchanged behavior).

IMPLEMENTATION:
  Counter: _rsl (register spill level) tracks current nesting depth.

  fn ESPILL(S) {
    var rsl = GRSL(S);
    if (rsl == 0) {
      # mov r12, rax (49 89 C4)
      EB(S, 0x49); EB(S, 0x89); EB(S, 0xC4);
    } else {
      EPUSHR(S);  # push rax (fallback)
    }
    SRSL(S, rsl + 1);
  }

  fn EUNSPILL(S) {
    var rsl = GRSL(S) - 1;
    SRSL(S, rsl);
    if (rsl == 0) {
      # First: mov rcx, r12 so result stays in rax, saved value goes to rcx
      # Actually: mov rax, r12 (4C 89 E0) then caller uses rax
      EB(S, 0x4C); EB(S, 0x89); EB(S, 0xE0);
    } else {
      EPOPR(S);   # pop rcx (fallback)
    }
  }

PROLOGUE/EPILOGUE CHANGES:
  Old: push rbp; mov rbp, rsp
  New: push rbx; push r12; push rbp; mov rbp, rsp
  Old epilogue: leave; ret
  New epilogue: leave; pop r12; pop rbx; ret

  ETAILJMP updated to match: leave; pop r12; pop rbx; jmp rel32.

STACK PARAM OFFSET:
  Was: [rbp+16] for 7th param (above saved rbp + return addr)
  Now: [rbp+32] for 7th param (above saved rbx + r12 + rbp + return addr)
  ESTOREPARM updated accordingly.

AARCH64:
  ESPILL/EUNSPILL are stubs that always use stack (push/pop fallback).
  ARM has plenty of callee-saved registers (x19-x28) but the optimization
  is deferred — stack spill works correctly for now.
'''

[[entries]]
name = "ret2_rethi_implementation"
description = "Return-by-value small structs: ret2(a,b) returns rax:rdx, rethi() reads rdx (v1.10.0)"
category = "cyrius"
content = '''
TOKENS: ret2 = 97, rethi = 98.

ret2(a, b) STATEMENT:
  Returns two values in rax (first) and rdx (second).
  Codegen:
    1. Evaluate b → rax
    2. mov rdx, rax (save b in rdx)
    3. Evaluate a → rax
    4. Epilogue (leave; pop r12; pop rbx; ret)
  After return: caller has a in rax, b in rdx.

rethi() EXPRESSION:
  Reads rdx from the most recent function call.
  Codegen: mov rax, rdx (48 89 D0)
  Returns the "high" value from the last ret2 call.

USAGE PATTERN:
  fn divmod(a, b) {
    var q = a / b;
    var r = a % b;
    ret2(q, r);
  }
  var result = divmod(17, 5);   # rax = 3 (quotient)
  var remainder = rethi();       # rax = 2 (remainder, was in rdx)

DISPATCH:
  Both tokens handled in PARSE_SIMD_EXT (dispatch function for tokens 93-98).
  ret2 is also recognized in PARSE_STMT expression range 62-98.

LIMITATION:
  rethi() must be called immediately after the function that used ret2.
  Any intervening function call or expression evaluation may clobber rdx.
  No compiler enforcement — programmer discipline required.
'''

[[entries]]
name = "simd_expand_v1100"
description = "4 new packed f64 SIMD ops: div, sqrt, abs, fmadd (v1.10.0)"
category = "cyrius"
content = '''
NEW OPS:
  f64v_div(dst, a, b, n)      — divpd xmm0, xmm1 (token 93)
  f64v_sqrt(dst, src, n)      — sqrtpd xmm0, xmm0 (token 94)
  f64v_abs(dst, src, n)       — andpd xmm0, [mask] (token 95)
  f64v_fmadd(dst, a, b, c, n) — mulpd + addpd (token 96)

EMIT HELPERS:
  EMIT_F64V_UNARY(S, opcode):
    Used for sqrt (0x51) and abs (custom). Emits loop:
      movupd xmm0, [src + i*16]
      <op> xmm0, xmm0
      movupd [dst + i*16], xmm0
    Loop increment by 2 (packed f64 = 2 elements per 128-bit register).

  EMIT_F64V_FMADD(S):
    Fused multiply-add without FMA3 hardware (software emulation):
      movupd xmm0, [a + i*16]
      movupd xmm1, [b + i*16]
      mulpd xmm0, xmm1
      movupd xmm1, [c + i*16]
      addpd xmm0, xmm1
      movupd [dst + i*16], xmm0
    Uses mulpd+addpd pair. True FMA3 (vfmadd231pd) deferred to future.

  f64v_abs MASK:
    andpd with 0x7FFFFFFFFFFFFFFF repeated in both lanes.
    Clears sign bit of both packed f64 values simultaneously.

DISPATCH:
  All 4 ops dispatched via PARSE_SIMD_EXT, called from PARSE_TERM
  when ptyp >= 93 && ptyp <= 98.

TOTAL SIMD OPS: 7 (add, sub, mul from v1.9.0 + div, sqrt, abs, fmadd from v1.10.0).
'''

[[entries]]
name = "parse_simd_ext"
description = "PARSE_SIMD_EXT dispatch function for tokens 93-98 (v1.10.0)"
category = "cyrius"
content = '''
PURPOSE:
  Tokens 93-98 (f64v_div, f64v_sqrt, f64v_abs, f64v_fmadd, ret2, rethi) are
  dispatched from a single function to avoid cluttering PARSE_TERM and PARSE_STMT
  with 6 separate branches.

DISPATCH:
  fn PARSE_SIMD_EXT(S, ptyp) {
    if (ptyp == 93) { ... f64v_div ... }
    elif (ptyp == 94) { ... f64v_sqrt ... }
    elif (ptyp == 95) { ... f64v_abs ... }
    elif (ptyp == 96) { ... f64v_fmadd ... }
    elif (ptyp == 97) { ... ret2 ... }
    elif (ptyp == 98) { ... rethi ... }
    return 0;
  }

CALL SITE (PARSE_TERM):
  Early check before the main token dispatch:
    if (ptyp >= 93) {
      if (ptyp <= 98) {
        return PARSE_SIMD_EXT(S, ptyp);
      }
    }
  This avoids adding 6 elif branches to the already-large PARSE_TERM.

PARSE_STMT INTEGRATION:
  Expression range expanded from 62-92 to 62-98.
  When PARSE_STMT sees a token in this range, it dispatches to expression
  parsing (which includes PARSE_SIMD_EXT for 93-98).
'''

[[entries]]
name = "lexkw_ext"
description = "LEXKW_EXT helper: extended keyword checks for tokens 93-98 (v1.10.0)"
category = "cyrius"
content = '''
PROBLEM:
  LEXID identifies keywords by comparing identifier strings against known names.
  Adding 6 more keywords (f64v_div through rethi) caused LEXID to exceed the
  compiler's code size budget for a single function — too many string comparisons
  in sequence.

FIX:
  Extracted extended keyword checks (tokens 93-98) into LEXKW_EXT(kw, klen, S, ws).
  Called from LEXID after the main keyword check returns 0 (not a base keyword).

  fn LEXKW_EXT(kw, klen, S, ws) {
    # Check for f64v_div, f64v_sqrt, f64v_abs, f64v_fmadd, ret2, rethi
    # Returns token number (93-98) or 0 if not a keyword
    if (klen == 7) {
      if (... matches "f64v_div" ...) { return 93; }
      if (... matches "rethi" ...) { ... }
    }
    ...
    return 0;
  }

INTEGRATION:
  In LEXID, after the main keyword chain:
    var ext = LEXKW_EXT(kw, klen, S, ws);
    if (ext > 0) { ADDTOK(S, ext, ws); return ...; }
  Falls through to normal IDENT token if ext == 0.

PATTERN:
  This is the same factoring approach used for ASM_MNEMONIC — when a single
  function grows too large, extract a subset into a helper. The compiler has
  no function size limit per se, but large functions push against token count
  and identifier buffer limits during self-compilation.
'''

[[entries]]
name = "ref_directive"
description = "#ref preprocessor directive: load TOML key-value pairs at compile time (v1.10.0)"
category = "cyrius"
content = '''
SYNTAX:
  #ref "config.toml"

EFFECT:
  Reads the TOML file and emits var key = value; for each key-value line.
  Skips comment lines (#), section headers ([section]), and blank lines.
  String values are emitted as string literals. Integer values as numbers.

EXAMPLE:
  # config.toml
  version = 42
  name = "myapp"

  # After #ref expansion, source contains:
  var version = 42;
  var name = "myapp";

IMPLEMENTATION:
  PP_REF_PASS runs BEFORE PP_PASS (before include expansion).
  Scans input for lines matching #ref "...".
  ISREF(S, ip, bl) detects the directive (similar to ISINCLUDE).

  For each #ref line:
    1. Extract filename from quotes
    2. Read file contents via syscall(2, open) + syscall(0, read)
    3. Parse line by line:
       - Skip if line starts with # or [
       - Skip blank lines
       - Find = separator
       - Emit: var <key> = <value>;
    4. Write generated var declarations to output buffer

PASS ORDER:
  PP_REF_PASS → PP_PASS (includes) → PP_IFDEF_PASS → PP_MACRO_PASS → LEX

USE CASE:
  Configuration files that define constants for a program.
  Avoids hand-maintaining duplicate var declarations.
  Phase 1: simple key=value only. No nested tables, no arrays.
'''

[[entries]]
name = "hashmap_simplification"
description = "Hashmap code simplified: removed enum, extracted lookup helper, used elif (v1.10.0)"
category = "cyrius"
content = '''
CHANGES:
  1. Removed enum constants for entry states. Now uses literal values:
     0 = empty, 1 = occupied, 2 = tombstone (was: EMPTY, OCCUPIED, TOMBSTONE).
     Reduces identifier count and avoids enum init dependency.

  2. Extracted _map_lookup(m, key) helper function:
     Shared probe logic used by map_get, map_has, map_delete.
     Returns slot index if found (state==1 and key matches), or -1.
     Eliminates 3 copies of the same linear probe loop.

  3. Used elif in probe loop:
     if (state == 0) { break; }
     elif (state == 1) { if (key matches) { return slot; } }
     elif (state == 2) { /* skip tombstone */ }
     Cleaner than nested if/else chains.

RESULT:
  Fewer lines, same behavior. All existing hashmap tests pass.
  Tombstone semantics preserved (see hashmap_tombstone entry).
'''

[[entries]]
name = "inline_small_functions_reverted"
description = "HISTORICAL: Token replay inlining added v1.10.0, disabled v1.11.3 — I-cache pressure made code slower"
category = "cyrius"
content = '''
STATUS: DISABLED (v1.11.3, _INLINE_OK = 0). Metadata tables still allocated but unused.

Token replay inlined 1-param functions with <= 6 body tokens by replaying
their body tokens at the call site. Generated larger code per site than
the 5-byte `call` it replaced, hurting I-cache on real workloads.

CRITICAL BUG during development:
  PARSE_STMT had a token range check (tokens 62-92) that silently skipped
  new tokens. Initial debugging focused on lexer (wrong lead) — the real
  bug was PARSE_STMT's range check not including new tokens.

KEY LESSON:
  When adding new tokens/keywords, check ALL range checks: PARSE_STMT,
  PARSE_TERM, PARSE_FACTOR all have hardcoded token ranges.
'''

[[entries]]
name = "function_code_size_limits"
description = "Compiler silently truncates large functions — extraction pattern (v1.10.0-v1.11.0)"
category = "cyrius"
content = '''
PROBLEM:
  The compiler has implicit code size limits per function. When a function
  generates more machine code than the internal buffer/region allows,
  the excess is silently dropped. No error message, no warning — the
  function just produces wrong code or missing handlers.

AFFECTED FUNCTIONS:
  LEXID       — keyword recognition. Too many keywords overflowed the
                if/elif chain. Extracted to LEXKW_EXT for extended keywords.
  PP_PASS     — preprocessor. Too many directives. Extracted PP_REF_PASS
                as a separate pre-pass.
  PARSE_TERM  — term-level expression parsing. New builtins exceeded
                capacity. Extracted to PARSE_SIMD_EXT for SIMD/math tokens.
  PARSE_STMT  — statement parsing. Similar overflow. Range checks needed
                expansion AND handler extraction.

SOLUTION PATTERN:
  When a function approaches its code generation limit:
  1. Identify a logical subset of functionality (e.g., "extended keywords")
  2. Extract to a new helper function (e.g., LEXKW_EXT)
  3. Add a single call/branch from the original function to the helper
  4. The helper handles the extracted cases and returns to the caller

  This is the compiler equivalent of "extract method" refactoring, but
  the motivation is code generation limits rather than readability.

DETECTION:
  Symptoms are always silent wrong behavior:
  - New keywords not recognized (LEXID overflow)
  - New preprocessor directives ignored (PP_PASS overflow)
  - New builtins parse as identifiers (PARSE_TERM overflow)
  - New statement forms silently skipped (PARSE_STMT overflow)

  If a newly added feature "doesn't work" but the token is lexed correctly,
  check whether the handling function has hit its size limit.

KEY LESSON:
  This is a fundamental constraint of the self-hosting architecture.
  The compiler compiles itself, so the code generation limits apply to
  the compiler's own code. There is no easy way to raise the limits
  without a multi-step bootstrap (compile with old limits, then recompile
  with new limits). The extraction pattern is the sustainable approach.
'''


[[entries]]
name = "aarch64_native_self_host"
description = "aarch64 native self-hosting — inline metadata segfault and _INLINE_OK fix (v1.11.0)"
category = "cyrius"
content = '''
PROBLEM:
  The aarch64 native compiler (not cross-compiler, but compiler running
  natively on ARM hardware) segfaulted when inline function support was
  enabled. The inline metadata tables live at fixed addresses (0x2C8000+)
  that are within the brk region on x86_64 but generated incorrect
  addresses when the compiler ran on ARM.

SYMPTOM:
  Segfault during compilation when the compiler tried to write inline
  metadata (fn_body_start, fn_body_end, fn_inline) at addresses that
  were not mapped on the ARM system.

ROOT CAUSE:
  The inline metadata code generated x86-specific absolute addresses.
  On ARM, the memory layout differs (different brk behavior, different
  page sizes in some cases). The addresses that worked under qemu
  (which emulates x86 memory layout) failed on real ARM hardware.

FIX:
  Added _INLINE_OK flag. When _INLINE_OK == 0:
  - Skip all inline metadata writes during function definition
  - Never take the inline path in PARSE_FNCALL
  - All functions use normal call/ret convention

  The aarch64 compiler sets _INLINE_OK = 0. The x86_64 compiler
  sets _INLINE_OK = 1 (or leaves it default-enabled).

  This means aarch64 binaries are slightly larger (no inlining) but
  correct. Inlining support for aarch64 would require porting the
  metadata tables to ARM-appropriate addresses.

KEY LESSON:
  Always test native ARM builds on real hardware. qemu user-mode
  emulation does not faithfully reproduce memory mapping behavior.
  A program that works under qemu may segfault on real ARM due to
  different brk/mmap address ranges. The _INLINE_OK pattern (feature
  flag to disable platform-specific optimizations) is reusable for
  any future x86-only feature.
'''

[[entries]]
name = "derive_codegen_fixes"
description = "Derive codegen bugs: field buffer, arg count, directive skipping (v1.10.0-v1.11.0)"
category = "cyrius"
content = '''
OVERVIEW:
  #derive(Serialize) generates _to_json() and _from_json() methods for
  structs. Several bugs were found and fixed across v1.10.0-v1.11.0.

BUG 1: Field name buffer overflow (16→32 bytes)
  Field names longer than 16 characters overflowed the name buffer in
  the derive code generator, corrupting adjacent data. Increased buffer
  to 32 bytes to handle longer field names like "background_color".

BUG 2: Single-arg _to_json (was 2-arg)
  The generated _to_json function signature had 2 parameters (struct
  pointer + output buffer), but callers only passed 1 argument (the
  struct pointer). This caused a segfault because the second parameter
  contained garbage from whatever was in the register.
  Fix: changed _to_json to single-arg (struct pointer only), with the
  output buffer allocated internally.

BUG 3: PP_DERIVE_SERIALIZE must skip intervening #derive lines
  When multiple #derive directives appear before a struct:
    #derive(Serialize)
    #derive(Debug)
    struct Foo { x; y; }
  PP_DERIVE_SERIALIZE would try to parse the second #derive as the
  struct definition and fail. Fix: skip any #derive lines between the
  triggering #derive(Serialize) and the actual struct keyword.

BUG 4: PP_DERIVE_DESER is now no-op
  Originally, Serialize and Deserialize were separate derive passes.
  But PP_DERIVE_SERIALIZE already emits both _to_json and _from_json.
  Making PP_DERIVE_DESER a no-op avoids duplicate generation.

KEY LESSON:
  Code generation (generating source code from directives) is fragile
  because errors manifest in the GENERATED code, not where the bug
  actually is. Debug by printing the generated source (before compilation)
  to see what the derive pass actually produced.
'''

[[entries]]
name = "freelist_allocator"
description = "Segregated free-list allocator with mmap-backed arena (v1.11.0)"
category = "cyrius"
content = '''
OVERVIEW:
  A segregated free-list allocator providing fl_alloc/fl_free/fl_calloc
  for dynamic memory management. Replaces raw mmap/munmap for small
  allocations, reducing system call overhead.

DESIGN:
  9 size classes: 16, 32, 48, 64, 96, 128, 256, 512, 4096 bytes.
  Each size class has its own free list (singly-linked list of blocks).

  Small allocations (<=4096):
    1. Round up requested size to the next size class
    2. Check the free list for that class
    3. If free list non-empty: pop head, return it
    4. If free list empty: carve from the arena (mmap-backed region)

  Large allocations (>4096):
    Direct mmap with size rounded up to page boundary.
    fl_free for large blocks calls munmap directly.

ARENA:
  A single large mmap region serves as the backing store for small blocks.
  A bump pointer tracks the next free position. When the arena is exhausted,
  a new arena is mmap'd. Old arenas are not freed (they contain live blocks
  that will eventually be returned to free lists).

fl_free:
  1. Determine size class from block header (stored in 8 bytes before
     the returned pointer, or passed explicitly)
  2. Push block onto the free list head for that size class
  3. No coalescing — freed blocks stay at their size class permanently

fl_calloc:
  fl_alloc + memset to zero. Used for arrays and structs that need
  zero-initialization.

KEY LESSON:
  The segregated free-list avoids fragmentation within size classes
  and makes alloc/free O(1) for the common case (free list hit).
  The trade-off is internal fragmentation (a 17-byte request uses
  a 32-byte block) and no coalescing (memory is not returned to the
  OS for small blocks). For a compiler/systems context where
  allocation patterns are predictable, this is a good trade-off.
'''

[[entries]]
name = "thread_async_primitives"
description = "Threads via clone+mmap, futex mutex, MPSC channels, async runtime (v1.11.0-v1.11.1)"
category = "cyrius"
content = '''
OVERVIEW:
  Low-level concurrency primitives built directly on Linux syscalls.
  No pthreads, no libc. Everything goes through syscall instruction.

THREADS:
  Thread creation uses clone() syscall with a mmap'd stack region.
  Each thread gets its own stack (typically 8MB mmap with MAP_STACK).
  clone flags: CLONE_VM | CLONE_FS | CLONE_FILES | CLONE_SIGHAND |
  CLONE_THREAD | CLONE_SYSVSEM.

FUTEX MUTEX:
  Mutex built on futex(FUTEX_WAIT, FUTEX_WAKE) syscalls.
  Three states: 0=unlocked, 1=locked-no-waiters, 2=locked-with-waiters.
  Lock: cmpxchg 0→1 (fast path), else set to 2 and FUTEX_WAIT.
  Unlock: xchg to 0, if old state was 2 then FUTEX_WAKE(1).
  This is the standard futex-based mutex algorithm.

MPSC CHANNELS:
  Multi-producer single-consumer channels using:
  - Ring buffer for message storage
  - Futex for consumer blocking
  - Mutex for producer synchronization
  Send: lock, write to ring buffer, unlock, futex_wake.
  Recv: futex_wait until data available, read from ring buffer.

ASYNC RUNTIME:
  Event loop built on epoll + timerfd:
  - epoll_create1 for the event loop
  - timerfd_create + timerfd_settime for async sleep
  - Fork-based timeout: fork(), child sleeps then exits, parent
    continues — if parent doesn't complete, child's exit signals timeout

CRITICAL BUG — pipe() stack corruption:
  pipe(pipefd) writes two 32-bit file descriptors (8 bytes total).
  var pipefd[2] allocates a 2-BYTE array (Cyrius arrays are byte-sized).
  The pipe() syscall writes 8 bytes into a 2-byte buffer, corrupting
  6 bytes of adjacent stack data.
  Fix: var pipefd[8] (or var pipefd[16] for safety).

KEY LESSON:
  When calling Linux syscalls that write structs to user buffers, always
  check the ACTUAL size of the struct, not the element count. pipe()
  returns two int32s = 8 bytes. This is a class of bug that affects any
  syscall with output parameters: stat (144 bytes), sigaction (152 bytes),
  etc. Always allocate based on byte size, not logical element count.
'''

[[entries]]
name = "lexhex_buffer_bug"
description = "LEXHEX reads from raw input instead of preprocessed buffer — documented known issue (v1.11.0)"
category = "cyrius"
content = '''
PROBLEM:
  LEXHEX (hex constant lexer, handles 0x... literals) reads from S+p
  (the raw input buffer) instead of S+0x222000+p (the preprocessed buffer).
  All other lexer functions read from the preprocessed buffer.

WHY IT WORKS TODAY:
  PP_IFDEF_PASS copies the preprocessed content back to S+0 (the raw input
  position) after preprocessing is complete. So by the time LEXHEX runs,
  S+p and S+0x222000+p contain identical content.

WHEN IT WOULD BREAK:
  If any preprocessor pass removed or relocated content that contains hex
  constants, and that pass did NOT copy results back to S+0, LEXHEX would
  read stale/wrong data from the raw input buffer.

  Example scenario: if #ifdef removed a block containing `var x = 0xFF;`
  and the pass wrote results only to the preprocessed buffer (S+0x222000),
  LEXHEX would still see the 0xFF in the raw buffer and try to lex it,
  even though it was #ifdef'd out.

WHY NOT FIXED:
  Changing LEXHEX to read from S+0x222000+p requires updating every
  address calculation in the hex lexer. There are too many references
  to the raw buffer offset pattern, and the risk of introducing a
  regression outweighs the benefit (since the current behavior is
  correct for all existing preprocessor passes).

STATUS:
  Documented known issue. Not fixed. If a future preprocessor pass
  breaks the "copy back to S+0" invariant, this will need to be
  addressed. The fix is mechanical (add 0x222000 to all buffer offsets
  in LEXHEX) but tedious and high-risk.

KEY LESSON:
  Sometimes the correct engineering decision is to document a latent bug
  rather than fix it. The fix is straightforward but touches many places,
  and the bug only triggers under conditions that don't currently exist.
  Document the invariant that must hold (PP passes copy back to S+0)
  and revisit if that invariant is ever broken.
'''

[[entries]]
name = "multi_file_compilation_design"
description = "v2.0 target: .o object files + linking — design research"
category = "cyrius"
content = '''
STATUS: Design research. Implementation depends on v1.12 hardened heap.

GOAL:
  Compile individual .cyr files to ELF .o relocatable object files, then
  link them together. Enables: separate compilation, faster rebuilds,
  library distribution as .o/.a files.

CURRENT MODEL:
  Single compilation unit. `include "file.cyr"` textually inserts the file
  into the input stream. The compiler sees one giant source. All symbols
  are resolved internally via the fixup table.

WHAT .o FILES NEED:

  1. SYMBOL TABLE
     Each .o needs a list of defined symbols (functions, globals) and
     undefined symbols (external references). Currently the compiler has
     fn_names/fn_offsets but these are internal indices, not ELF symbols.

     New: emit ELF .symtab section with STT_FUNC / STT_OBJECT entries.
     Each function gets a symbol. Each global var gets a symbol.

  2. RELOCATION ENTRIES
     Currently fixup entries are resolved internally (all addresses known).
     With .o files, some addresses are unknown (defined in other .o files).
     These become ELF relocation entries (R_X86_64_PC32 for calls,
     R_X86_64_64 for absolute addresses).

     The fixup table already stores the needed info (code offset + type +
     target). Converting fixup entries to ELF relocations is mechanical.

  3. SECTION HEADERS
     Current ELF output has no sections (just a single LOAD segment).
     .o files need: .text (code), .data (globals), .rodata (strings),
     .symtab, .strtab, .rela.text.

  4. LINKER
     Need a minimal linker that: reads multiple .o files, resolves symbols,
     patches relocations, emits final executable. Could be written in Cyrius.

IMPLEMENTATION PLAN:

  Phase 1 — ELF .o emission:
    - Add `-c` flag to compiler: emit .o instead of executable
    - EMITELF_OBJ function: emit proper ELF with sections
    - Convert fixup entries to relocation entries for undefined symbols
    - Self-test: compile one .cyr to .o, link with simple stub

  Phase 2 — Minimal linker:
    - Read ELF .o files (parse headers, sections, symbols, relocations)
    - Symbol resolution: match undefined refs to definitions
    - Patch relocations with final addresses
    - Emit executable ELF (same format as current EMITELF_USER)

  Phase 3 — Incremental compilation:
    - cyrb tracks which .cyr files changed
    - Only recompile changed files to .o
    - Relink all .o files
    - Faster rebuilds for large projects

KEY INSIGHT:
  The fixup table IS a relocation table. Converting it to ELF relocations
  is straightforward:
    fixup type 0 (var addr) → R_X86_64_64
    fixup type 1 (string addr) → R_X86_64_64
    fixup type 2 (fn call) → R_X86_64_PC32
    fixup type 3 (fn pointer) → R_X86_64_64
  The main new work is: section headers, symbol table, and the linker.

SCOPE FOR v2.0:
  Phase 1 only — emit .o files. Linker can be a separate v2.0 milestone
  or use the system ld as an interim solution.
'''

[[entries]]
name = "cyrius_x_bytecode_design"
description = "v2.1 target: cyrius-x portable bytecode format — design research"
category = "cyrius"
content = '''
STATUS: Design research for v2.0 scaffold, full implementation in v2.1.

GOAL:
  A portable bytecode format that compiles once, runs anywhere.
  Not WASM — owned by Cyrius, optimized for Cyrius semantics.
  The VM interprets or JIT-compiles cyrius-x bytecode.

DESIGN DECISIONS:

  1. REGISTER-BASED (not stack-based)
     Cyrius already uses a register-first model (rax, rcx, stack spill).
     A register VM maps 1:1 from the existing codegen. Stack-based VMs
     (like WASM/JVM) require an extra compilation step from register IR.
     Register VMs are also faster to interpret (fewer ops per operation).

     Reference: Lua 5.0 switched from stack to register VM for 20-30% speedup.
     Reference: Dalvik (Android) was register-based for the same reason.

  2. FIXED-WIDTH INSTRUCTIONS (32-bit)
     Each instruction is 4 bytes: opcode (8 bits) + operands (24 bits).
     Simple to decode, cache-friendly, easy to validate.
     Operand encoding: 8-bit register IDs (256 virtual registers),
     16-bit signed immediates, or 24-bit branch offsets.

  3. TYPE TAGS IN BYTECODE
     Each operation specifies width: add.i8, add.i16, add.i32, add.i64.
     The VM can validate type safety without a separate type system.
     Maps directly to the multi-width types added in v2.0.

  4. SYSCALL ABSTRACTION
     Native syscalls are platform-specific. cyrius-x replaces them with
     a host function table. The VM provides: read, write, open, close,
     brk, exit, mmap, etc. Portable across Linux/macOS/Windows.

INSTRUCTION SET (draft):

  Arithmetic:  add, sub, mul, div, mod (with .i8/.i16/.i32/.i64 suffix)
  Bitwise:     and, or, xor, not, shl, shr
  Compare:     eq, ne, lt, gt, le, ge → sets register to 0/1
  Memory:      load.N, store.N (N = 8/16/32/64)
  Control:     jmp, jz, jnz, call, ret
  Stack:       push, pop (for spill/restore)
  System:      syscall N (index into host function table)
  Immediate:   movi (load immediate into register)
  Address:     lea (load effective address)

  ~30 opcodes × 4 width variants = ~120 instruction encodings.

FILE FORMAT (.cyx):

  Header: magic (4 bytes "CYX\0") + version + entry_point offset
  Code section: instruction stream
  Data section: global variables + string literals
  Symbol table: function names + offsets (for debugging)
  No relocations needed — all addresses are bytecode offsets.

IMPLEMENTATION PLAN:

  v2.0 — Scaffold:
    - Define instruction encoding in docs
    - Add cyrius-x backend stub (emit.cyr for bytecode)
    - Emit simple programs to .cyx files
    - Write minimal interpreter in Cyrius (self-referential!)

  v2.1 — Full implementation:
    - Complete instruction set
    - VM with full syscall abstraction
    - All stdlib tests pass under VM
    - Performance: target 10-50x slower than native (acceptable for portability)

  v2.2 — cyrius-ts:
    - TypeScript parser → cyrius-x bytecode
    - JS runtime hosts cyrius-x VM (via wasm or native addon)

KEY INSIGHT:
  The existing compiler already has a clean frontend/backend split.
  Adding a cyrius-x backend means implementing emit_cx.cyr that emits
  bytecode instead of x86/aarch64 machine code. The frontend (lex, parse)
  is completely reused. The fixup table handles bytecode offsets the same
  way it handles native offsets.

SELF-HOSTING POTENTIAL:
  If the VM is fast enough, the compiler itself could run under cyrius-x.
  This means: compile on any platform, bootstrap on any platform.
  The 29KB seed binary becomes a cyrius-x binary + a small native VM.
'''

[[entries]]
name = "bug14_heap_collision"
description = "Bug #14: SIGSEGV on 6000+ line programs — heap offset collision between && patches and continue patches (v1.11.4)"
category = "cyrius"
content = '''
SYMPTOM:
  cc2 crashes with SIGSEGV (exit 139), 0-byte output, no error on stderr.
  Only on programs > ~6000 lines after include expansion. Compiler's own
  source (~6000 lines) compiled fine. Argonaut (6257 lines) crashed.

DIAGNOSIS:
  1. Built diagnostic compiler with stderr prints before each PARSE_FN_DEF
  2. Identified crash inside init_current_stage function
  3. Narrowed to line 110 (for-loop closing brace)
  4. Found the function uses `status != A && status != B && status != C`
     inside a for-in collection loop
  5. Triple && works in small programs — crash is size-dependent

ROOT CAUSE:
  Heap offset collision between two arrays:
    extra_patches (&&/|| chaining): 0x8F848, 8 entries × 8 bytes → 0x8F888
    continue counter:               0x8F850 (INSIDE extra_patches[1])
    continue patches:               0x8F858 (INSIDE extra_patches[2..9])

  With `a && b && c`, ADDXP writes second patch to 0x8F848+1*8 = 0x8F850,
  overwriting the continue counter with a code position (~36000).

  At for-loop close: `var cfwn = L64(S + 0x8F850) - 1` reads ~36000.
  Loop iterates 36000 times: `EPATCH(S, L64(S + 0x8F858 + i * 8))`.
  EPATCH writes to `S + 0x20000 + off` where off is garbage from heap.
  When garbage value exceeds brk boundary → unmapped address → SIGSEGV.

WHY SIZE-DEPENDENT:
  In small programs, code positions are small (~100-5000). The corrupted
  continue count is small, and the EPATCH loop stays within allocated
  heap — writes garbage to garbage but doesn't crash. In large programs,
  code positions are ~36000+, and the loop reads past brk.

FIX (v1.11.4):
  Relocated continue data to non-overlapping region:
    continue counter:  0x8F850 → 0x8F8A0
    continue patches:  0x8F858 → 0x8F8A8

  Verified with two-step bootstrap. All 267 tests pass. Argonaut
  (300 tests + 39 serde + 18 benchmarks) compiles and runs.

  v1.11.5 added overflow guards: ADDXP checks c >= 8, continue patches
  check ci >= 8. Both error cleanly instead of silent overflow.

KEY LESSON:
  The compiler's flat heap layout makes offset collisions invisible.
  Two features can independently test clean but collide when combined.
  Bug #14 required BOTH triple && AND for-loop AND large program size.
  A heap offset audit tool that flags overlaps would catch this class
  of bug at commit time.

  Diagnosis approach: build a diagnostic compiler variant with stderr
  prints at key phases (preprocess, lex, pass 1, pass 2 per-function).
  This avoids needing strace/gdb on a minimal binary with no debug info.
'''

[[entries]]
name = "dce_bitmap_optimization"
description = "DCE optimization from O(N×T) to O(T+N) using referenced-name bitmap (v1.11.5)"
category = "cyrius"
content = '''
PROBLEM:
  Dead code elimination in pass 2 scanned ALL tokens for every function
  definition — O(N×T) where N=functions, T=tokens. For argonaut
  (358 functions, 36K tokens), this was ~13M iterations per compilation.
  For bhava (500+ functions, 100K+ tokens), it would be 50M+.

OLD APPROACH (O(N×T)):
  For each `fn name()`:
    if name has no underscore (not mangled):
      scan all tokens: if any IDENT == name (not after FN token): reachable
      if not found: emit stub (xor eax,eax; ret), skip body

NEW APPROACH (O(T+N)):
  Before pass 2, build a referenced-name bitmap in one pass:
    1. Zero 8KB bitmap in output_buf (S+0x6A000) — safe scratch, EMITELF runs later
    2. For each token: if IDENT and not preceded by FN, set bit at name_offset
    3. Bitmap: 65536 bits = 8192 bytes, indexed by tok_names offset

  In pass 2, for each `fn name()`:
    if name has no underscore:
      check bitmap bit at name_offset — O(1) lookup
      if not set: emit stub, skip body

  Total: O(T) for bitmap build + O(N) for function checks = O(T+N)

WHY IT WORKS:
  Lexer deduplication guarantees identical identifier strings share the
  same tok_names offset. So if `foo` appears as both a definition (after
  FN) and a call (not after FN), both tokens have the same TOKVAL. The
  bitmap marks the call's offset, and the definition checks the same offset.

SCRATCH SPACE:
  Uses output_buf (S+0x6A000, 128KB) for the 8KB bitmap. This buffer is
  unused until EMITELF runs after all compilation is complete. No heap
  layout change or brk extension needed.

TWO-STEP BOOTSTRAP:
  Required — the new compiler generates different code (bitmap init loop
  before pass 2). Old cc2 → new cc2_a → cc2_b → cc2_c. cc2_b == cc2_c.
'''

[[entries]]
name = "overflow_guards_pattern"
description = "Pattern: adding bounds checks to flat heap arrays — v1.11.5 hardening"
category = "cyrius"
content = '''
PROBLEM:
  The compiler uses fixed-size arrays at hardcoded heap offsets. Most had
  no bounds checking — overflow silently corrupted adjacent state. Bug #14
  was caused by exactly this: ADDXP writing past extra_patches into
  continue patches.

PATTERN — adding a guard:
  Before:
    fn ADDXP(S, p) {
        var c = GXPC(S);
        S64(S + 0x8F848 + c * 8, p);
        SXPC(S, c + 1);
        return 0;
    }

  After:
    fn ADDXP(S, p) {
        var c = GXPC(S);
        if (c >= 8) {
            syscall(SYS_WRITE, 2, "error: too many && conditions (max 8)\n", 38);
            syscall(SYS_EXIT, 1);
        }
        S64(S + 0x8F848 + c * 8, p);
        SXPC(S, c + 1);
        return 0;
    }

ARRAYS GUARDED (v1.11.5):
  ADDXP — extra_patches for && chaining (max 8)
  continue patches — for-loop continue (max 8)
  ret_patches — return statements per function (max 64)
  REGSTRUCT — struct definitions (max 32)

STILL UNGUARDED (v1.12 target):
  struct fields (16 per struct) — overflows silently into struct_count/loop state
    Currently benign because loop state is reinitialized in pass 2.
    Needs heap relocation to expand safely.
  var_noffs, fn_names, fn_offsets — large arrays, unlikely to overflow

KEY LESSON:
  Every flat array needs a bounds check. The check costs 2 instructions
  and adds ~20 bytes of code per guard. The alternative is silent heap
  corruption that may not crash until unrelated code runs — the worst
  kind of bug to diagnose in a system with no debugger or symbol table.
'''

[[entries]]
name = "struct_field_overflow"
description = "Struct field limit (16) silently overflows into loop state — needs v1.12 heap relocation"
category = "cyrius"
content = '''
CURRENT LAYOUT:
  struct_fnames at 0x8E830: 32 structs × 16 fields × 8 bytes = 4096 bytes
  Ends at 0x8F830 (struct_count).
  struct_ftypes at 0x8A000: same layout, 4096 bytes, ends at 0x8B000.

  Indexing: si * 128 + fi * 8 (stride 128 = 16 fields × 8 bytes)

THE OVERFLOW:
  argonaut has ServiceDefinition with 21 fields. At field 17+, writes go to:
    0x8E830 + si*128 + 16*8 = 0x8E830 + si*128 + 128 = next struct's slot
  For the last struct (si=31): 0x8E830 + 31*128 + 128 = 0x8F830 = struct_count
  Fields 17-21 overwrite struct_count, loop_top, break_patch, extra_patches.

WHY IT DOESN'T CRASH:
  Struct parsing happens in pass 1. Loop state (loop_top, break_patch) is
  initialized fresh for each loop in pass 2. The overwritten values are
  replaced before they're used. The extra_patches overwrite is benign
  because no && conditions are being evaluated during struct parsing.

FIX (v1.12):
  Expand stride from 128 to 256 (32 fields per struct).
  This requires moving struct_fnames to a new location — the current
  region (0x8E830-0x8F830) only has 4096 bytes.
  Options:
    1. Move struct_fnames after fn_inline (0x2CE000+) and extend brk
    2. Move loop state/extra_patches forward to make room
  Either requires a two-step bootstrap.

KEY LESSON:
  "Benign overflow" is a time bomb. It works today because of incidental
  ordering (pass 1 overwrites, pass 2 reinitializes). Any change to
  compilation phases could turn this into a silent miscompilation.
'''

[[entries]]
name = "heap_consolidation"
description = "v2.1: Heap consolidation 6.8MB→4.7MB with 132 offset relocations"
category = "cyrius"
content = '''
BEFORE: Heap grew to 6.8MB with scattered regions, many gaps.
AFTER: Consolidated layout starting at 0xA0000 for high-heap tables.

KEY RELOCATIONS:
  fixup_tbl      → 0xA0000 (131072 bytes, 8192 entries)
  fn_names       → 0xC0000
  fn_offsets     → 0xC2000
  fn_params      → 0xC4000
  fn_body_start  → 0xC6000
  fn_body_end    → 0xC8000
  fn_inline      → 0xCA000
  struct_fnames  → 0xCC000
  output_buf     → 0xCE000 (262144 bytes)
  var_noffs      → 0x10E000
  var_sizes      → 0x11E000
  var_types      → 0x12E000
  tok_types      → 0x13E000 (131072 entries)
  tok_values     → 0x23E000
  tok_lines      → 0x33E000
  preprocess_out → 0x43E000 (512KB)
  brk            → 0x4BE000

132 offset references updated across 6 source files.
Every change requires two-step bootstrap (cc3 compiles cc4, cc3==cc4).
'''

[[entries]]
name = "str_replace_str_api_bug"
description = "v2.6.1: str_replace used strlen() on Str objects — silent data corruption"
category = "cyrius"
content = '''
BUG: regex.cyr str_replace() called strlen(old) and strlen(new_str) on Str
  objects. Str stores {data_ptr, length} — strlen on the Str pointer reads
  the raw pointer bytes as ASCII until it hits a null, producing garbage length.

SYMPTOM: str_replace("hello world", "world", "cyrius") returned the
  original string unchanged — the memeq comparison at the wrong length
  never found a match.

FIX: Replace strlen(old) → str_len(old), strlen(new_str) → str_len(new_str).
  Use str_data() to get raw pointer for memeq/memcpy.

LESSON: Cyrius has two string types — C strings (char*) and Str (ptr+len).
  Functions that accept one must not be called with the other.
  Convention: C string params are usually named `s` or `cstr`.
  Str params are named with type annotation or documented.
'''

[[entries]]
name = "sha1_buffer_overflow"
description = "v2.6.3: SHA-1 var w[80] was 80 bytes for 640 bytes of data"
category = "cyrius"
content = '''
BUG: majra/src/ws.cyr sha1() declared `var w[80]` for the message schedule.
  SHA-1 needs 80 × 32-bit words = 320 bytes minimum.
  Code used store64/load64 (8 bytes per entry) → 80 × 8 = 640 bytes needed.
  With only 80 bytes allocated, writes to w[10+] clobbered stack variables
  (h0-h4, padded, block, and locals from the calling function).

SYMPTOM: Garbled output after sha1() call. The `println` after sha1 produced
  backticks and control characters because the format buffer was clobbered.
  The SHA-1 output appeared correct by accident — the clobbered h0-h4 values
  happened to produce the right digest through overlapping memory reads.

FIX: Changed to heap allocation: `var w = alloc(640)`.
  Updated all `&w + offset` to `w + offset`.

KEY GOTCHA: `var buf[N]` in Cyrius is N BYTES, not N elements.
  For an array of 80 i64 values, you need `var buf[640]` not `var buf[80]`.
  This is documented in Known Gotchas but still catches every new project.
'''

[[entries]]
name = "port_validation_pattern"
description = "v2.7: Port validation sweep — compile and test all downstream repos against current cc2"
category = "cyrius"
content = '''
Before each release, validate all downstream repos compile and test clean.

PATTERN:
  for repo in agnostik agnosys argonaut sakshi majra libro bsp cyrius-doom; do
    cd ../$repo && cat src/main.cyr | cc2 > /tmp/test && ./tmp/test
  done

KEY INSIGHT: Must cd into each repo first — include paths are relative to CWD.
Running `cat /abs/path/to/main.cyr | cc2` from the wrong directory causes
"undefined variable" errors for all enum constants because the includes
resolve to the wrong lib/ directory.

RESULTS (v2.7.3):
  agnostik: 223 passed   argonaut: compiles clean   libro: 193 passed
  majra: 144 passed       bsp: 74 passed             sakshi: 12 passed
  agnosys: compiles       cyrius-doom: compiles (129KB binary)
  Total: 646 assertions across 5 test suites, 0 failures.

AUTOMATION: `cyrius soak` runs this in a loop for overnight validation.
'''

[[entries]]
name = "cx_syscall_address_translation"
description = "v2.7: cyrius-x VM syscall string output — data section mapping"
category = "cyrius"
content = '''
PROBLEM: syscall(1, 1, "hello", 5) in cyrius-x bytecode produces no output.
  The string address in the bytecode is a virtual offset, but the host
  syscall needs a real memory pointer.

TWO-PART FIX:

1. CX COMPILER (main_cx.cyr):
   - Added FIXUP phase that was completely missing from cx output
   - Computes data layout: [bytecode (cp)] [var data (totvar)] [string data (spos)]
   - Patches movi instructions with correct data offsets:
     string addr = code_size + total_var_size + string_offset
     var addr = code_size + cumulative_var_offset
   - Appends var data + string data after bytecode in .cyx file
   - GOTCHA: fixup coff points AFTER movi, so patch at coff-2 for bytes 2-3

2. CX VM (cxvm.cyr):
   - Copies entire .cyx content into _cx_mem so bytecode offsets work as
     memory addresses for load/store operations
   - Syscall handler translates virtual addresses to real pointers:
     write(1): r3 (buf) translated via _cx_mem + offset
     read(0): r3 (buf) translated
     open(2): r3 (path) translated
   - Only pointer args translated, not length/flag args (would corrupt values)

VERIFIED: "hello cx" prints correctly. fib(10)=55, fact(5)=120 still pass.
'''

[[entries]]
name = "prefix_sum_fixup"
description = "v2.9/3.0: O(n²)→O(n) fixup optimization with prefix-sum variable offsets"
category = "cyrius"
content = '''
BEFORE: Each variable address fixup computed cumulative offset by scanning
  all preceding variables: while (vi < idx) { cumul += var_sizes[vi]; }
  With 4096 fixups and 256 variables = O(1M) operations.

AFTER: Precompute prefix sums in one O(n) pass before the fixup loop.
  Store in scratch area (output_buf tail, safe since ELF not written yet).
  Each fixup now does a single L64(pfx + idx * 8) lookup.

Added bounds check: if (idx < vcnt) before the prefix sum lookup.
Prevents silent corruption from invalid fixup entries.
'''

[[entries]]
name = "bug31_prline_segfault"
description = "v2.8.2: Bug #31 — struct field on undefined var segfaults due to nonexistent PRLINE function"
category = "cyrius"
content = '''
BUG: `var r = q.x;` where q is undefined caused SIGSEGV instead of error.

ROOT CAUSE: Four error handlers in PARSE_FIELD_LOAD and PARSE_FIELD_STORE
  called `PRLINE(S)` — a function that was never defined. The compiler
  silently generated a call to address 0 (unresolved function), causing
  SIGSEGV when the error handler tried to print the line number.

FIX: Replaced all 4 instances with the correct pattern:
  syscall(SYS_WRITE, 2, "error:", 6);
  PRNUM(GTLINE(S, GTI(S)));
  syscall(SYS_WRITE, 2, ": undefined variable ...");

LESSON: The compiler does not error on undefined function calls at compile
  time — it emits a warning but generates a call to offset 0. This is by
  design (forward references), but means typos in internal function names
  produce silent runtime crashes, not compile errors.
'''

[[entries]]
name = "hashmap_fast_api_parity"
description = "v2.8/3.0: hashmap_fast.cyr API parity — delete, keys, values, clear"
category = "cyrius"
content = '''
hashmap_fast.cyr (SIMD Swiss table) was missing 4 functions that hashmap.cyr had:
  fhm_delete(m, key) — tombstone slot (set metadata byte to 0xFF)
  fhm_keys(m)        — scan metadata, collect non-empty/non-tombstone keys
  fhm_values(m)      — same for values
  fhm_clear(m)       — memset metadata to 0, reset count

All 4 follow the same metadata scan pattern: iterate cap slots,
check load8(meta + i) != 0 && != 255 for occupied entries.

8 new assertions in hashmap_ext.tcyr (delete, keys count, values count, clear).
'''

[[entries]]
name = "output_too_large_diagnostics"
description = "v2.8.1: Improved output-too-large error with code/data/strings breakdown"
category = "cyrius"
content = '''
BEFORE:
  error: output too large (280000/262144 bytes)

AFTER:
  error: output too large (280000/262144 bytes)
    code: 180000 data: 95000 strings: 5000
    tip: heap-allocate large var buf[N] arrays

PLUS: Warning at >128KB static data:
  warning: large static data (140000 bytes) — consider alloc() for buffers >4KB

Triggered by argonaut adding sakshi_full (4.5KB static arrays) on top of
a 64KB load_env_file buffer. The agent fix was correct: heap-allocate
large buffers. The compiler now tells you proactively.
'''

[[entries]]
name = "defer_implementation"
description = "v3.2: defer statement codegen — inline blocks with epilogue chain"
category = "cyrius"
content = '''
DESIGN:
  defer { body } compiles the block inline (jumped over during normal flow).
  At function return, a chain of jmp instructions routes through each
  deferred block in LIFO order before reaching EFNEPI.

CODEGEN:
  1. When `defer {` parsed: emit JMP-OVER, record block start + end-jmp
  2. Parse block body (compiled inline like any statement block)
  3. Emit JMP placeholder (end-jmp, patched at epilogue time)
  4. Store {block_start, end_jmp} in defer table (max 8 per function)
  5. Patch JMP-OVER to skip past the block

  At epilogue:
  1. Return patches land here
  2. Save return value: EPUSHR (push rax)
  3. For each defer LIFO: emit EJMP(block_start), EPATCH(end_jmp)
  4. Restore return value: EPOPR (pop rax)
  5. EFNEPI (normal epilogue)

HEAP: defer_count at 0x8F900, defer_addrs at 0x8F908 (16 bytes/entry)
TOKEN: 106 for `defer` keyword
LIMIT: 8 defer blocks per function (same as continue patches)

KEY DECISION: Return value must be preserved across defer chain.
  Without push/pop rax, `return 42;` with defer returns 0.
'''

[[entries]]
name = "derive_str_implementation"
description = "v3.2: #derive(Serialize) Str field detection in preprocessor"
category = "cyrius"
content = '''
The preprocessor detects `: Str` type annotations by checking 4 bytes:
  if (load8(&field_types + fi * 32) == 83)       # 'S'
    if (load8(&field_types + fi * 32 + 1) == 116) # 't'
      if (load8(&field_types + fi * 32 + 2) == 114) # 'r'
        if (load8(&field_types + fi * 32 + 3) == 0)  # null terminator

For _to_json: Str fields emit:
  str_builder_add_cstr(sb, "\"");
  str_builder_add_cstr(sb, str_data(load64(ptr + offset)));
  str_builder_add_cstr(sb, "\"");

For _from_json: Str fields emit:
  if (v != 0) { store64(ptr + offset, str_from(v)); }

LIMITATION: Only "Str" is recognized as a string type.
  Custom string types need manual _to_json/_from_json.

BEFORE: All fields treated as i64 → Str fields showed pointer addresses.
AFTER: Str fields show quoted string content in JSON output.
'''

[[entries]]
name = "function_table_expansion_3x"
description = "v3.2: Function table 1024→2048 — 15 heap offset relocations"
category = "cyrius"
content = '''
agnostik hit the 1024 function limit with 852 src + 84 test + stdlib functions.
Expanding required doubling all 6 function tables from 8KB to 16KB each.

IMPACT: 15 heap regions shifted. Every offset reference across 12 source files updated.
  fn_offsets: 0xC2000 → 0xC4000 (11 occurrences in parse.cyr alone)
  fn_params:  0xC4000 → 0xC8000
  fn_body_*:  shifted by +4000 each
  All downstream: struct_fnames, output_buf, var tables, token arrays

PATTERN: Same as every heap expansion — two-step bootstrap required.
  The offsets are baked into the compiled cc2 binary. Changing them means
  cc2 (compiled with old offsets) produces cc3 (with new offsets),
  then cc3 produces cc4 (also with new offsets), and cc3==cc4.

LESSON: Function count grows faster than expected in downstream projects.
  agnostik went from 200 functions (v0.91) to 850+ (v0.97) in one month.
  Each #derive(Serialize) generates 2 functions per struct.
'''

[[entries]]
name = "regalloc_peephole"
description = "#regalloc per-fn hot-local routing via post-emit peephole (v4.8.4)"
category = "cyrius"
content = '''
Cyrius ships a single-register hot-local allocator gated on a per-fn
`#regalloc` directive. End-to-end in v4.8.4 (alpha1..beta1); extended
with use-count picker + width-aware safety scan in v4.8.4-alpha8.

PIPELINE (seven stages):

  1. LEX. `#regalloc` (line-start) lexes as token 109. Silent in pass 1.
  2. PARSE metadata. `fn_regalloc[4096]` table at 0xC8000, accessed via
     `GFRA` / `SFRA`. Pass 2's decl dispatcher arms a pending flag on
     token 109; `PARSE_FN_DEF` transfers it to `fn_regalloc[fi]` when
     it resolves `fi`, then clears pending. DCE-stub path clears it
     without applying (dead fns have no prologue to attach to).
  3. MIRROR. `_cur_fn_regalloc` global mirrors `fn_regalloc[fi]` for
     the duration of a fn body; emit helpers read it cheaply.
  4. FRAME LAYOUT. `#regalloc` fns bump `flc` by 1 pre-rounding so the
     `(flc*8+15) & -16` formula still yields a 16-aligned frame. The
     extra slot sits at `[rbp-8]`; EVERY user-visible local disp is
     shifted by -8. Seven emit sites touched: `EFLLOAD`, `EFLSTORE`,
     `EFLLOAD_W`, `EFLSTORE_W`, `ESTOREPARM`, `ELVRINIT`, and the
     `lea rax, [rbp+disp32]` address-of path.
  5. PROLOGUE / EPILOGUE. After `sub rsp, fsz`, emit
     `mov [rbp-8], rbx` (48 89 5D F8). Before `leave; ret`, emit
     `mov rbx, [rbp-8]` (48 8B 5D F8). `ETAILJMP` mirrors the restore
     before its synthetic `mov rsp, rbp; pop rbp; jmp`.
  6. USE-COUNT PICKER (post-body). Scan bytes `fn_start..GCP` for
     `48 8B 85 <disp32>` and `48 89 85 <disp32>` patterns, count
     occurrences per local slot. Pick the non-param slot with max
     count (threshold ≥ 2). Cap at flc ≤ 256 locals.
  7. SAFETY SCAN + PATCH. Before patching the picked slot, scan for
     any `85 <hot_disp>` at a position whose two preceding bytes are
     NOT `48 8B` or `48 89` — that's a pattern we can't rewrite
     (`lea` for &local, `movzx` for i8/i16 load, `88/66 89/89` width-
     aware store, `48 8B A5` r12 loop-cache). Abort routing if any
     unsafe pattern hits. Otherwise rewrite all matching 7-byte
     mov's: load → `mov rax, rbx` (48 89 D8), store → `mov rbx, rax`
     (48 89 C3), followed by one 4-byte NOP `0F 1F 40 00` so jump
     offsets stay anchored.

OBSERVED WIN. Nested accumulator microbench: ~12% faster than plain.
Tight single-accumulator sum: parity (memory access already overlaps
with ALU). FNV-style xor/mul loop: neutral (mul latency hides the
memory access).

DESIGN TRADE-OFFS (WORTH UNDERSTANDING):
- Single-register by design. Multi-reg (r12..r15) deferred to 4.9.x —
  frame layout infrastructure accommodates the extension, but the
  picker + patcher would need to juggle four candidate slots without
  cross-slot conflicts.
- Post-emit peephole (not IR-based). Forces the NOP padding to keep
  offsets anchored. Eliminating the padding requires a real layout
  pass; that's a 5.x-era concern.
- The compiler's OWN frame size grew by ~8 bytes per fn that picks up
  regalloc. Negligible absolute cost; disciplined use is still
  opt-in.

LESSON. The heap-map entry for `fn_regalloc` at 0xC8000 (32 KB)
documents the metadata table — any table this size needs a named
slot in src/main.cyr's heap map BEFORE code writes to it.
'''

[[entries]]
name = "u128_hardware_fastpath"
description = "u128_divmod b_hi==0 hardware fast-path via asm block (v4.8.5-alpha1/alpha2)"
category = "cyrius"
content = '''
`lib/u128.cyr::u128_divmod` takes a shortcut when the divisor fits
in 64 bits. Covers every `(u64 * u64) % u64` pipeline — Miller-Rabin,
Pollard rho, RSA, cryptographic hashing, `random::shuffle`. abaco's
ntheory::mod_mul was hand-rolling a double-and-add loop because the
pre-alpha1 path burned 128 shift-subtract iterations even when the
top half of the divisor was zero.

SHAPE (v4.8.5-alpha1):

    if (b_hi == 0) {
        # Two hardware `div` steps back-to-back, both inside one asm
        # block so both run UNSIGNED (Cyrius's `/` operator lowers to
        # idiv — signed — which mis-computes step 1 whenever a_hi has
        # its top bit set, exactly mulmod's common case).
        var q_hi_fast = 0;
        var q_lo_fast = 0;
        var r_final   = 0;
        asm {
            0x48; 0x8B; 0x45; 0xD0;   # mov rax, [rbp-48] (a_hi)
            0x48; 0x31; 0xD2;         # xor rdx, rdx
            0x48; 0xF7; 0x75; 0xC8;   # div qword [rbp-56] (b_lo)
            0x48; 0x89; 0x45; 0xB8;   # mov [rbp-72], rax (q_hi_fast)
            0x48; 0x8B; 0x45; 0xD8;   # mov rax, [rbp-40] (a_lo)
            0x48; 0xF7; 0x75; 0xC8;   # div qword [rbp-56]
            0x48; 0x89; 0x45; 0xB0;   # mov [rbp-80], rax (q_lo_fast)
            0x48; 0x89; 0x55; 0xA8;   # mov [rbp-88], rdx (r_final)
        }
        ...
    }

KEY INSIGHTS:
1. Cyrius `/` AND `%` ARE SIGNED. Don't use them for the step-1
   division even when the divisor is positive — a_hi with top bit
   set silently produces a tiny signed remainder that overflows
   step 2's hardware div and trips #DE. Do BOTH steps in asm, both
   unsigned.
2. Same asm handles a_hi==0 AND a_hi!=0. When a_hi==0, step 1
   returns q_hi=0, r1=0, and step 2 degrades to a single 64-bit
   `div`. No separate branch needed.
3. `r1 < b_lo` by definition of the first remainder, so step 2
   never trips #DE (quotient always fits in 64 bits).
4. rbp-disp offsets are hardcoded to the compiler's local indexing
   — document them in a comment next to the asm block, and update
   if you add locals before the asm.

COMPANION APIs (v4.8.5-alpha2):
- `u64_mulmod(a, b, m)` — asm-direct `mul; div; mov`, three
  instructions. Preconditions: m > 0, a < m, b < m. Violating
  preconditions trips #DE at the div.
- `u64_powmod(base, exp, m)` — square-and-multiply built on
  `u64_mulmod`. ~60 inner iterations for primality-grade moduli.

MEASURED:
  mulmod/binary_slow: 618 ns avg (pre-alpha1 pure-Cyrius shape)
  mulmod/u64_fast:    402 ns avg  ← 1.54× primitive
  miller_rabin/slow:   11 µs avg
  miller_rabin/fast:  956 ns avg  ← ~12× on a full MR round

LESSON FOR ADDING ASM-INTEGRATED STDLIB HELPERS. The asm-block
idiom in Cyrius is powerful when (a) the fn body has no other
locals that shift the offsets, (b) the ABI you're calling (here,
Cyrius's rbp-relative locals) is stable and auditable, (c) both
signed and unsigned semantics are considered for every arithmetic
step. The `u128_divmod` fast-path is the cleanest current example;
the pattern is reusable for other hardware-accelerated primitives
(SHA extensions, AES-NI, BMI2 `mulx`, etc.).
'''


[[entries]]
name = "plan_win64_abi_v553"
description = "v5.5.3 ✅ SHIPPED — Win64 arg-register flip for ≤4-arg cyrius-to-cyrius fn calls (EPOPARG + ESTOREREGPARM under _TARGET_PE)"
category = "cyrius"
content = '''
SHIPPED 2026-04-20 as v5.5.3. Scope was NARROWED from the
original full-ABI-lift plan to just the arg-register flip (≤4
args). Shadow space + >4-arg stack shuttle + fnptr.cyr Win64
variants split into v5.5.4 (see plan_win64_abi_completion_v554).

WHAT ACTUALLY SHIPPED (~20 lines in src/backend/x86/emit.cyr):

- EPOPARG _TARGET_PE branch (lines 745-767): n=0 → pop rcx
  (0x59), n=1 → pop rdx (0x5A), n=2 → pop r8 (0x5841), n=3 →
  pop r9 (0x5941). n≥4 falls through to SysV encoding.
- ESTOREREGPARM _TARGET_PE branch (lines 852-880): symmetric
  flip on the callee side. pidx=0 → mov [rbp+disp], rcx
  (0x8D8948), pidx=1 → mov [rbp+disp], rdx (0x958948), pidx=2 →
  mov [rbp+disp], r8 (0x85894C), pidx=3 → mov [rbp+disp], r9
  (0x8D894C). pidx≥4 falls through.

VERIFIED on real Windows 11 (nejad@hp, build 26200): 5/5 fncall
matrix PASS — 1-arg (RCX), 2-arg (RCX+RDX), 3-arg (+R8), 4-arg
(+R9), nested (fn-calling-fn). hello.exe still runs (v5.4.x
reroutes unaffected). Linux check.sh 10/10 green, 70 .tcyr
suites PASS, self-host byte-identical (479440 B, +832 B over
v5.5.2).

CI GATE: `.github/workflows/ci.yml` `windows-cross` job's new
"v5.5.3 Win64 ABI fncall gate" step asserts byte-level presence
of the Win64 spill quartet (48898d / 4889?? / 4c8985 / 4c898d)
in w_arg4.exe. `windows-native` runs each program on
`windows-latest`, asserts ERRORLEVEL=42 for all five. Catches
any future regression that drifts back to SysV regs.

KEY LESSON: Cyrius's codegen doesn't use RDI/RSI inside fn
bodies (only at the arg-pass/arg-receive boundary). So flipping
EPOPARG + ESTOREREGPARM symmetrically was the ENTIRE coherent
change needed for ≤4-arg calls to work — no prologue/epilogue
changes, no callee-save flip, no shadow space required (both
sides are cyrius code; cyrius doesn't use shadow space
internally either). This made the first slice 20 lines instead
of the 200 originally estimated.

ORIGINAL FULL-ABI PLAN (now v5.5.4 scope) kept for reference.

SYSV → WIN64 DIFFERENCES (the canonical reference): Today under CYRIUS_TARGET_WIN=1 every Win32
reroute (EEXIT / EWRITE_PE / v5.5.1 bundle / v5.5.2 enum-const fold)
already sets up Win64 ABI correctly at the kernel32 boundary, but
cyrius-to-cyrius function calls still use SysV x86_64 inside the
compiled binary. That "half-Win64" state doesn't crash as long as
user code never invokes Win32 callbacks, but breaks the moment we
do (WNDPROC, thread-start routines, CRT hooks). v5.5.3 lifts the
internal convention to match the boundary convention.

SYSV → WIN64 DIFFERENCES (the canonical reference):

  SysV x86_64 (Linux/Mach-O)         Win64 (Microsoft x64)
  ─────────────────────────          ─────────────────────
  int args: RDI,RSI,RDX,RCX,R8,R9    int args: RCX,RDX,R8,R9
  (first 6)                          (first 4 only)
  fp args: XMM0..XMM7 (first 8)      fp args: XMM0..XMM3 (first 4)
  args 7+: stack                     args 5+: stack (at [rsp+32+])
  no shadow space                    32 B mandatory shadow space
  128 B red zone below RSP           NO red zone (RSP-volatile)
  caller-saved: RAX,RCX,RDX,RSI,     caller-saved: RAX,RCX,RDX,R8,R9,
    RDI,R8..R11,XMM0..XMM15            R10,R11,XMM0..XMM5
  callee-saved: RBX,RBP,R12..R15     callee-saved: RBX,RBP,RDI,RSI,
    (no XMM callee-saves)              RSP,R12..R15,XMM6..XMM15
  return: RAX (int), XMM0 (fp)       return: RAX (int), XMM0 (fp)
  struct >16 B: mem retptr arg 0    struct >8 B: hidden retptr in RCX,
    (RDI), args shift                   other args shift right one slot
  variadic fp: XMM only              variadic fp: duplicate to both
                                        XMM and corresponding GP reg

CYRIUS-SPECIFIC DETOUR — the flip is cheaper than it looks:

1. Cyrius's codegen does NOT use RDI/RSI inside compiled function
   bodies. They only appear at the arg-pass/arg-receive boundary
   (EPOPARG, ESTOREPARM) and are dead by the time the body runs.
   Callee-save-set flip (SysV→Win64 adds RDI/RSI) is therefore a
   NO-OP inside EFNPRO/EFNEPI — we don't save/restore them because
   we don't use them.
2. XMM6..XMM15 callee-saved flip is also a no-op today — Cyrius
   emits no SSE inside compiled user code (the optimizer phases in
   v5.5.x haven't landed yet). When SSE codegen ships, revisit.
3. RDI/RSI as incoming arg registers: under Win64 they ARE NOT args
   any more — args 5 and 6 come from [rsp+32+offset] instead. The
   ONLY change needed at EPOPARG is to route n=0,1,2,3 → RCX,RDX,R8,
   R9 and n=4,5,6,7 → stack reads.
4. Cyrius's aarch64 backend stays untouched — different file, Linux
   stays on SysV, Mach-O stays on SysV. v5.5.3 is a pure _TARGET_PE
   gate.

CALL-SITE CHANGES (src/backend/x86/emit.cyr):

  EPOPARG(S, n)                   lines 745-753
    Today: n=[0..5] → [0x5F, 0x5E, 0x5A, 0x59, 0x5841, 0x5941]
           (pop rdi/rsi/rdx/rcx/r8/r9).
    Win64: n=[0..3] → pop into RCX/RDX/R8/R9; n=[4..7] → leave
           args on stack at [rsp+32+(n-4)*8] (caller already wrote
           them there per parse.cyr's push loop).
    Guard: `if (_TARGET_PE == 1)` branch at top.

  ECALLPOPS(S, n)                 lines 761-787
    Today: pops ≤6 args into SysV registers; >6 uses r11-r14 as
           temps and re-pushes in correct order.
    Win64: pops ≤4 args into RCX/RDX/R8/R9; args 5+ already live
           on stack — caller leaves them there, emits
           `sub rsp, 32` to open shadow space, `call`, then
           `add rsp, 32+(N-4)*8` on the ECALLCLEAN side. No
           re-push needed because stack args stay at [rsp+32+].

  ECALLCLEAN(S, n)                lines 790-801
    Today: `add rsp, (n-6)*8` for n>6.
    Win64: `add rsp, 32 + max(0, (n-4))*8` unconditionally —
           every call pays the 32 B shadow tax. n≤4: `add rsp, 32`.
           n=5: `add rsp, 40`. n=6: `add rsp, 48`.

  ESTOREPARM(S, pidx, lidx, pc)   lines 862-873
  ESTOREREGPARM / ESTORESTACKPARM lines 840-859
    Today: pidx<6 → register spill into [rbp+disp]; pidx≥6 →
           stack read from [rbp+16+i*8].
    Win64: pidx<4 → RCX/RDX/R8/R9 spill into [rbp+disp];
           pidx≥4 → stack read from [rbp+16+32+(pidx-4)*8]
           (the 32 accounts for the shadow space the caller
           allocated below the return address).

RSP ALIGNMENT INVARIANT (non-negotiable):
  RSP must be 16-aligned at every `call` instruction. Entry to
  a function (immediately after `call` pushes return addr) has
  RSP ≡ 8 (mod 16). EFNPRO's `push rbp` returns RSP ≡ 0 (mod 16).
  Any `sub rsp, N` must keep N a multiple of 16. EEXIT today uses
  `sub rsp, 0x28` (40 = 32 shadow + 8 pad); EWRITE_PE uses
  `sub rsp, 0x40` (64 = 32 shadow + 32 locals). Generalize: if
  the fn has K qword spill slots, frame = `max(32, 32 + K*8)`
  rounded up to next 16.

fnptr.cyr CHANGES (lib/fnptr.cyr):
  fncallN (N=0..8) currently use `#ifdef CYRIUS_ARCH_X86` inline
  asm that hardcodes RDI/RSI/... Under Win64 self-host they need
  a parallel `#ifdef CYRIUS_TARGET_WIN` path with Win64 registers
  + shadow-space `sub rsp, 0x20` before the indirect call. Affects
  lines 43-322 — 9 asm blocks total. Can be gated by the same
  #ifdef the backend checks (_TARGET_PE == 1 at cc5_win compile).

SELF-HOST FIXPOINT (the authoritative gate):
  cc5_v2 == cc5_v1 (Linux, today) — unchanged.
  Add cc5_win_v2 from cc5_win_v1 under CYRIUS_TARGET_WIN=1, assert
  cc5_win_v1 == cc5_win_v2 byte-identical. This runs in the
  ubuntu-cross path since cc5_win runs BY cc5; actual Windows-
  native fixpoint (cc5_win compiling itself ON Windows) lands in
  v5.5.4 (plan_windows_self_host_v554).

3-FAILED-ATTEMPTS FALLBACK (per CLAUDE.md):
  If self-host fixpoint fails three distinct Win64 migration
  attempts, split the work:
    v5.5.3a — arg-register mapping only (EPOPARG + ECALLPOPS
              + ESTOREPARM).
    v5.5.3b — shadow space + ECALLCLEAN.
    v5.5.3c — callee-save set flip (likely a no-op for Cyrius).
  Each sub-patch self-hosts independently.

SCOPE NOT INCLUDED (now v5.5.4, see plan_win64_abi_completion_v554):
  - Shadow-space allocation at cyrius-to-cyrius call sites.
  - >4-arg stack shuttle (ECALLPOPS/ECALLCLEAN/ESTORESTACKPARM).
  - fnptr.cyr Win64 fncallN variants.
  - Callee-save set flip (expected no-op for Cyrius — RDI/RSI
    unused in codegen — but formally required by the ABI).

SCOPE NOT INCLUDED (v5.5.5+):
  - Win64 self-host ON Windows (v5.5.5, see plan_windows_self_host_v554).
  - Struct-return-by-value via hidden RCX retptr (defer until
    aggregate returns appear in Win-compiled code).
  - __chkstk for frames ≥ 4 KB (no Cyrius fn currently trips this;
    the 21 MB compiler state lives on the HEAP, not the stack).
  - Variadic float duplication (defer until vararg support on
    the PE arm — Cyrius has no varargs today).
'''


[[entries]]
name = "plan_win64_abi_completion_v554"
description = "Implementation plan for v5.5.4: shadow space at cyrius-to-cyrius call sites + >4-arg stack shuttle + fnptr.cyr Win64 fncallN variants"
category = "cyrius"
content = '''
Forward-looking plan for v5.5.4 — completes the internal Win64 ABI
lift that v5.5.3 started. Once this ships, cc5's own source compiles
correctly under CYRIUS_TARGET_WIN=1 regardless of fn arity, and
the native Windows self-host in v5.5.5 is unblocked.

WHY THIS IS SPLIT FROM v5.5.3:
- v5.5.3 needed only EPOPARG + ESTOREREGPARM (caller-callee reg
  flip). ~20 lines, verified on real Windows in one session.
- v5.5.4 touches ECALLPOPS / ECALLCLEAN / ESTORESTACKPARM / every
  call site that today computes RSP alignment assuming no shadow
  + fnptr.cyr's 9 hand-rolled asm blocks. ~200 lines, higher risk
  per the original plan's estimate.
- CLAUDE.md principle: "ONE change at a time — never bundle
  unrelated changes." Arg-reg flip and shadow space have different
  failure modes; shipping them as one release would conflate
  bisect signal.

SCOPE — all _TARGET_PE-guarded; Linux/Mach-O paths untouched:

1. SHADOW SPACE AT EVERY CYRIUS-TO-CYRIUS CALL SITE.
   Generalize the `sub rsp, N` pattern the kernel32 reroutes
   already use (EEXIT: sub rsp, 0x28; EWRITE_PE: sub rsp, 0x40).
   At each call site:
     - Before the call: `sub rsp, 0x28` (40 = 32 shadow + 8 align).
       RSP at the call must be 16-aligned; callee sees RSP ≡ 8
       (mod 16) after its `call` pushes the return addr.
     - After the call: `add rsp, 0x28` to unwind.
   Placement: ECALLPOPS appends sub rsp, 0x28 after all arg pops;
   ECALLCLEAN prepends add rsp, 0x28 before any stack-arg cleanup.

2. ECALLPOPS STACK-ARG SHUTTLE FOR N > 4.
   Mirror of the existing SysV N>6 shuttle (same file, same shape):
     - Pop the top (N-4) extras off the push-stack into scratch
       r10, r11, r14, r15 (or stash in memory if >4 extras).
     - Pop the 4 register-bound args into RCX/RDX/R8/R9 via EPOPARG.
     - Allocate shadow: sub rsp, 0x20.
     - Re-push extras so they land at [rsp+0x20..]: 5th arg goes
       to [rsp+0x20], 6th to [rsp+0x28], etc.
     - Call.
     - add rsp, 0x20 + (N-4)*8.

3. ESTORESTACKPARM DISP32 SHIFT.
   Under _TARGET_PE and pidx≥4:
     Today: stack-arg read from [rbp+16+8i] (SysV layout, no shadow).
     Win64: [rbp+16+32+(pidx-4)*8] — +32 accounts for caller's
     shadow space below the return address.
   Also ESTOREPARM's dispatch boundary shifts: pidx<4 → register
   path; pidx≥4 → stack path (vs SysV's pidx<6 / pidx≥6).

4. FNPTR.CYR WIN64 FNCALLN VARIANTS.
   lib/fnptr.cyr lines 43-322 have 9 hand-rolled x86 asm blocks
   (fncall0..fncall8) that load args from [rbp-disp] into
   RDI/RSI/.../R9. Add #ifdef CYRIUS_TARGET_WIN parallel blocks:
     - Args 0..3 → RCX/RDX/R8/R9.
     - For fncall5..fncall8, add sub rsp, 0x20 + extras × 8 before
       call, mov [rsp+0x20], arg4; [rsp+0x28], arg5; etc.
     - add rsp, N after call to unwind.
   Keep SysV blocks active under #ifdef CYRIUS_ARCH_X86 but NOT
   CYRIUS_TARGET_WIN so Linux/Mach-O are unaffected.

5. CALLEE-SAVE SET FLIP (expected no-op for Cyrius).
   Win64 adds RDI, RSI to callee-saved; SysV has them caller-saved.
   Verify via disasm of a cc5_win-compiled cc5 binary that no fn
   body touches RDI/RSI outside of EPOPARG/ESTOREREGPARM (which
   are at the arg boundary and immediately spill). If clean,
   EFNPRO/EFNEPI need no change. Add a guard comment.
   XMM6-XMM15 flip is similarly no-op until SSE codegen lands.

SELF-HOST FIXPOINT:
  - Linux cross: cc5 under CYRIUS_TARGET_WIN=1 produces cc5_win_v1.
  - cc5_win_v1 on Linux (as ELF, since we don't run on Windows in
    this release) compiles main_win.cyr → cc5_win_v2. Assert
    cc5_win_v1 == cc5_win_v2 byte-identical.
  - Actual Windows-native fixpoint is v5.5.5 scope.

3-FAILED-ATTEMPTS FALLBACK: if the full bundle fails, split
further — e.g. v5.5.4 = shadow space only, v5.5.5 = stack shuttle,
v5.5.6 = fnptr.cyr. Don't force.

WHAT BREAKS WITHOUT v5.5.4:
- Any Cyrius program under CYRIUS_TARGET_WIN=1 that calls a fn
  with 5+ args silently mis-compiles (args 5+ end up in wrong
  Win64 registers or at wrong stack offsets).
- Indirect fn-pointer calls via fncallN on Windows use wrong
  arg registers.
- Strict Win64 compliance (callees that DO use shadow space to
  spill inbound regs) breaks — though no kernel32 callee we've
  hit yet does this, so it's latent risk.
'''


[[entries]]
name = "plan_windows_self_host_v554"
description = "v5.5.5 implementation plan (filename retained from v5.5.4-era draft): native Windows self-host of cc5_win on windows-latest + full .tcyr suite"
category = "cyrius"
content = '''
Forward-looking plan for v5.5.5 (was v5.5.4 before the v5.5.3
Win64-ABI split; entry name retained to avoid cross-refs churn).
After v5.5.4 completes the internal Win64 ABI lift, cc5_win is a
complete PE32+ compiler that produces PE32+. The next gate is:
cc5_win compiling ITSELF on a real Windows host, byte-identical
to the ubuntu-cross build.

CURRENT STATE (v5.5.2 baseline):
  - .github/workflows/ci.yml has windows-cross + windows-native
    jobs. Cross-compile on ubuntu-latest with CYRIUS_TARGET_WIN=1,
    upload artifacts, run on windows-latest.
  - hello.exe round-trips end-to-end: "hi\\n" + exit 0.
  - exit0/exit1/exit42/exit255 gate passes on windows-latest.
  - cc5_win binary exists (v5.5.0 src/main_win.cyr cross-entry).

WHAT v5.5.4 MUST ADD:

1. cc5_win read-src-from-stdin on Windows
   - Native cc5 reads src/main.cyr from stdin (read(0, buf, ...)).
   - Win64 equivalent: GetStdHandle(STD_INPUT_HANDLE = -10 as DWORD
     = 0xFFFFFFF6) → ReadFile into heap buffer.
   - Parse-time reroute already shipped (v5.5.1 syscall(0, ...) →
     ReadFile via IAT). But it operates on fd=0; the GetStdHandle
     call is inserted by the PE backend when it detects fd=0 or fd=1.
   - No new compiler work — the existing v5.5.1 reroute handles it.
     Just verify ReadFile+pipe semantics match the Linux read() loop.

2. cc5_win write-bin-to-stdout on Windows
   - Cyrius's output model: PE bytes to stdout, redirect to a file.
   - PowerShell `>` REDIRECTION CORRUPTS BINARY OUTPUT. PowerShell
     reads stdout as text and re-encodes (UTF-16 BOM on 5.1, UTF-8
     BOM/no-BOM on 7+ depending on preference vars). NEVER use `>`
     on PE bytes. See field note windows_arc_anticipated_gotchas.
   - Correct forms (both work):
       cmd /c "type src\\main.cyr | cc5_win.exe > cc5_win_b.exe"
       Start-Process -FilePath cc5_win.exe -RedirectStandardInput src `
                     -RedirectStandardOutput cc5_win_b.exe `
                     -Wait -NoNewWindow
   - In GitHub Actions, prefer `shell: cmd` for the self-host step
     so redirection is byte-clean. The existing windows-native job
     uses `shell: pwsh` for Start-Process — keep that idiom; don't
     mix in `>` redirects.

3. File-include resolution on Windows
   - Cyrius's frontend preprocessor resolves `include "lib/foo.cyr"`
     by opening files directly (not via cyrius driver preprocessing).
   - Uses syscall(SYS_OPEN, path, flags, mode) → already rerouted
     to CreateFileW at v5.5.1. CreateFileW takes LPCWSTR (UTF-16)
     so the reroute needs to widen the ASCII path at emit time.
     Check: does the v5.5.1 sys_open reroute currently MultiByteToWideChar
     or assume ASCII-safe paths? If the latter, file a v5.5.4 sub-
     item for UTF-16 conversion.
   - Path separators: Win32 accepts both `/` and `\\` in paths, so
     Cyrius's `lib/foo.cyr` literals work without translation.
   - MAX_PATH (260 chars) applies unless paths start with `\\?\\` or
     the process opts in via registry. Our src tree fits under 260;
     non-issue today but document the ceiling.

4. Two-step bootstrap ON Windows
   - Round 1 (ubuntu-cross): cc5 + CYRIUS_TARGET_WIN=1 → cc5_win_v1.
   - Round 2 (ubuntu-cross repeat, still): cc5_win_v1 under wine or
     a container? No — run cc5_win_v1 on windows-latest via CI job.
     Input: src/main_win.cyr from the checkout. Output: cc5_win_v2.
   - Assert: cc5_win_v1 == cc5_win_v2 (cmp on Windows or artifact
     download + cmp on ubuntu).
   - Round 3 (windows-native): cc5_win_v2 compiles src/main_win.cyr
     a second time → cc5_win_v3. Assert cc5_win_v2 == cc5_win_v3.

5. .tcyr test suite on windows-latest
   - Each .tcyr compiled on Windows via cc5_win_v2, resulting binary
     executed on Windows, exit code checked.
   - Most .tcyr use syscall() with Linux numbers — the PE reroutes
     cover SYS_WRITE, SYS_READ, SYS_OPEN, SYS_CLOSE, SYS_LSEEK,
     SYS_MMAP, SYS_MUNMAP, SYS_EXIT. Tests using uncovered syscalls
     (fork, clone, futex, epoll, etc.) must be tagged
     `# platform: linux` or similar and skipped on Windows.
   - Shell harness in CI is pwsh; for-each loop over tests/tcyr/*.tcyr.
     Capture stdout + exit code, match against `# expect: ...` header
     or exit-code extract same as Linux flow.

GITHUB ACTIONS SHAPE (append to ci.yml after windows-native):

  windows-self-host:
    needs: [windows-native]
    runs-on: windows-latest
    steps:
      - uses: actions/download-artifact@v4
        with: { name: windows-pe-binaries, path: ./win }
      - name: Self-host round 2 (cc5_win_v1 → cc5_win_v2)
        shell: cmd
        run: |
          type src\\main_win.cyr | win\\cc5_win.exe > win\\cc5_win_b.exe
      - name: Byte-identical fixpoint
        shell: cmd
        run: fc /b win\\cc5_win.exe win\\cc5_win_b.exe
      - name: .tcyr suite
        shell: pwsh
        run: |
          # iterate tests/tcyr/*.tcyr, pipe to cc5_win_b.exe,
          # exec result, compare exit code to expectations.

WHAT WOULD BE WRONG IF THE FIXPOINT FAILS:
  - Byte diff in .text: ABI drift — some code path still emits SysV
    registers on the Windows side. Diff the two binaries' .text,
    disasm around the first diff.
  - Byte diff in .rdata: string or gvar ordering drift — check heap
    map + string interning determinism under the new code path.
  - Byte diff in .idata: IAT registration order changed — the hash
    used by _pe_ensure_* is deterministic; if order drifts, a cache
    or map iteration is non-deterministic.

SCOPE NOT INCLUDED (carries to v5.5.5+):
  - Windows as primary dev platform (still developing on Linux).
  - MSI / installer packaging (v5.6+ at earliest).
  - Windows long-path support (`\\?\\` prefix) — defer until needed.
  - UTF-16 path widening (if v5.5.1 reroute assumes ASCII, split
    into v5.5.4b sub-item).
'''


[[entries]]
name = "plan_compiler_optimization_v56x"
description = "Pinned release sequence for compiler optimization phases O1–O6. Each gets a concrete v5.6.x patch number — no more 'parallel track' drift across minors"
category = "cyrius"
content = '''
Compiler optimization (O1–O6) is its own minor at v5.6.x, landing
BEFORE RISC-V (v5.7.0, slid from v5.6.0) so the new port inherits
an optimized compiler rather than one still queueing baseline work.

PINNED RELEASE SEQUENCE (2026-04-20):
  v5.6.0 = Phase O1: instrumentation + FNV-1a symbol table (~240 LOC)
  v5.6.1 = Phase O2: peephole quick wins, x86_64 + aarch64 (~510 LOC)
  v5.6.2 = Phase O3: IR-driven passes (~590 LOC)
  v5.6.3 = Phase O4: linear-scan register allocation (~600–900 LOC)
  v5.6.4 = Phase O5: maximal-munch instruction selection (~300–500 LOC)
  v5.6.5 = Phase O6: slab allocator (~150 LOC, MEASUREMENT-GATED)
  v5.6.6 = v5.6.x closeout (or v5.6.5 if O6 skipped)

WHY THIS IS PINNED AND NOT "QUEUED":
  O1–O6 originally queued under v5.4.x as a "parallel track" (none
  shipped), carried forward to v5.5.x as the same "parallel track"
  (none shipped — v5.5.x focus stayed on platform completion).
  Without concrete patch numbers, the pattern would repeat: slip
  to v5.6.x "queue", slip to v5.7.x "queue", etc. Pinning to
  v5.6.0–v5.6.5 with explicit per-phase content makes the sequence
  auditable — a roadmap PR that skips a phase must account for it
  in CHANGELOG.

ORDERING CONSTRAINTS (non-negotiable):
  - O1 before everything: baseline numbers required to validate
    later phases quantitatively.
  - O3 before O4: linear-scan regalloc needs complete IR coverage
    (O3's precondition is finishing IR instrumentation across the
    remaining ~50 direct emit sites in parse.cyr).
  - O6 gated on O4 profile: slab only worth it if bump-alloc is
    hot during live-range construction. If not, SKIP and ship
    v5.6.5 as the closeout.

PREREQUISITE: v5.5.15 (v5.5.x closeout). No optimization phase
opens until v5.5.x platform + runtime work is honest-closed.

DOWNSTREAM DEPENDENCY: v5.7.0 (RISC-V rv64). The rv64 backend
lands its tile table against v5.6.4's maximal-munch walker on
day one, instead of retrofitting later. That's why RISC-V slid
from v5.6.0 to v5.7.0 on the same day this pinning happened.

NON-NEGOTIABLE CROSS-PHASE INVARIANTS:
  - Byte-identical self-host after every phase.
  - Every pass deterministic (no iterated coalescing, no
    load-order-dependent state).
  - No graph-coloring regalloc, no SCCP/GVN/polyhedral, no
    static instruction scheduling on x86_64, no PEXT/PDEP/BMI2
    opportunistic codegen. See roadmap §"v5.6.0 — Compiler
    optimization arc" guardrails.

RESEARCH SOURCES (converged):
  - QBE (simple backend, linear-scan regalloc shape)
  - TCC (one-pass codegen, shape of peephole in a small compiler)
  - QBE arm64 peephole — Brian Callahan
  - Poletto & Sarkar — linear-scan register allocation
  - Agner Fog — x86_64 microarchitecture (LEA latency, flag
    reuse, port pressure)
  - vidya content/optimization_passes, content/code_generation,
    content/allocators

KEY LESSON FROM v5.5.x PLATFORM ARC (carries to v5.6.x):
  Narrow, single-concern patches ship. Bundled patches stall.
  Each O-phase is independently verifiable (byte-identical
  self-host + benchmark numbers before/after) — ship them as
  distinct releases rather than as a "v5.6.0 optimizer drop".
'''


[[entries]]
name = "plan_pe_correctness_tail_v555plus"
description = "Deferred PE correctness items beyond v5.5.4: .reloc/ASLR, __chkstk, struct-return, variadic, SEH scope"
category = "cyrius"
content = '''
Items the Win64 ABI + native self-host arcs (v5.5.3 + v5.5.4) do
NOT deliver. Each is a distinct patch or minor; recorded here so
the triage doesn't rot between now and "someone trips it".

1. __chkstk / STACK PROBING
   Win64 reserves stack lazily — only the first page (4 KB) is
   committed at thread start, with a GUARD PAGE below. Accessing
   the guard page commits another 4 KB and moves the guard down.
   Touching memory BELOW the guard page (e.g. via `sub rsp, 8192`
   without intermediate loads) skips the guard and the kernel
   delivers STATUS_STACK_OVERFLOW.

   The fix: for any fn frame ≥ 4 KB, inject a `call __chkstk`
   after computing frame size in RAX. __chkstk walks the frame in
   4 KB steps, loading a byte from each page, forcing the guard
   sequence in order. MSVC injects this automatically; no-CRT
   compilers must supply their own.

   Minimal __chkstk implementation (36 bytes, public-domain, from
   Chris Wellons' libchkstk):
     push rax
     push rcx
     neg  rax
     add  rsp, rax          ; hypothetical new RSP
     mov  rcx, gs:[0x10]    ; StackLimit (TIB + 0x10)
     jmp  .check
   .probe:
     sub  rcx, 0x1000
     test eax, [rcx]        ; touch this page
   .check:
     cmp  rcx, rsp
     ja   .probe
     pop  rcx
     pop  rax
     ret

   CYRIUS STATUS: no function in cc5, no function in the stdlib,
   and no function in any downstream program currently has a stack
   frame ≥ 4 KB. The 21 MB compiler state lives on the HEAP (heap
   map in main.cyr). Stack frames are typically < 512 bytes. DEFER
   until a compiled program trips STATUS_STACK_OVERFLOW (if ever).

   WHEN IT MATTERS: if Cyrius grows a `var buf[4096];`-style large
   automatic array. Today's alternative (and the idiomatic one) is
   `var buf = alloc(4096)`, which is heap and untouched by the
   guard page mechanism.

2. .reloc SECTION + ASLR
   v5.4.2 sets IMAGE_FILE_RELOCS_STRIPPED (0x0001), so the loader
   must place the binary at ImageBase = 0x140000000. This works
   for a single CLI exe — the kernel always has that VA range free.

   For DLL output OR to opt into ASLR
   (IMAGE_DLLCHARACTERISTICS_DYNAMIC_BASE = 0x0040), emit a .reloc
   section listing every absolute 64-bit address in code/data.

   .reloc block format (per PE32+ spec):
     Block header (8 bytes):
       DWORD PageRVA       ; 4 KB-aligned RVA the block covers
       DWORD BlockSize     ; header + entries, DWORD-aligned
     Entries (2 bytes each):
       USHORT: top 4 bits = type, bottom 12 bits = offset within page
     Types for PE32+:
       IMAGE_REL_BASED_ABSOLUTE = 0  (padding only)
       IMAGE_REL_BASED_DIR64    = 10 (primary, 64-bit reloc)

   Example: 3 DIR64 relocs at RVA 0x1008, 0x1010, 0x1018:
     PageRVA   = 0x00001000
     BlockSize = 16       ; 8 header + 6 entries + 2 pad
     entries   = 0xA008, 0xA010, 0xA018, 0x0000

   When the loader picks a different ImageBase, it adds
   (actual - preferred) to every 64-bit slot listed in .reloc.

   CYRIUS STATUS: DIR64 sites in a Cyrius-compiled PE are:
   (a) every `movabs rax, imm64` targeting a gvar or string literal
       (v5.4.8 instrumented these already — use the fixup table);
   (b) every IAT-target `movabs` (but `call [rip+disp32]` via IAT
       is already PIC, no reloc needed);
   (c) the entry point address in the PE header.
   Estimated entry count: ~3-5 per fn, ~one per string literal.
   A typical self-hosted cc5 PE with ~475 KB and ~1200 fns would
   emit ~2000-3000 DIR64 entries ≈ 6-9 KB of .reloc.

   DEFER until first DLL output or compliance requirement for ASLR.

3. STRUCT-RETURN-BY-VALUE (>8 bytes)
   Win64 convention: return types > 8 B are returned via HIDDEN
   POINTER. Caller allocates space, passes the address as arg 0
   in RCX. All other args shift right one slot: arg 1 → RDX,
   arg 2 → R8, arg 3 → R9, arg 4+ on stack. Callee returns the
   same pointer in RAX.

   Cyrius's struct-return story today:
     - Structs ≤ 8 B: returned in RAX (works on both ABIs — the
       SysV and Win64 cases match).
     - Structs > 8 B: Cyrius today returns via the `#derive(accessors)`
       pattern + heap alloc; true pass-by-value struct return is
       unimplemented. No compiled code exercises the Win64 hidden-
       retptr convention yet.

   DEFER until an aggregate-return fn appears in Windows-compiled
   code. When that happens, the fix is a new EFNRET_PE branch that
   copies the local struct into [RCX] + sets RAX = RCX, plus a
   caller-side allocation stage in ECALLPOPS.

4. VARIADIC FLOAT DUPLICATION
   Win64 rule: float args passed to a VARIADIC or UNPROTOTYPED
   function must be loaded into BOTH the positional XMM register
   AND the corresponding integer register. So `printf("%f", 3.14)`
   needs `movsd xmm1, [rip+pi]; movq rdx, xmm1; call printf`.

   CYRIUS STATUS: Cyrius has NO variadic functions. fncallN is
   fixed-arity via the N suffix. DEFER until varargs ship (if ever).

5. SEH / .pdata / .xdata — INDEFINITELY DEFERRED
   Structured Exception Handling metadata. Not needed for CLI .exe
   execution. Only matters for:
   (a) C++ exceptions (Cyrius has none)
   (b) SEH __try/__except (Cyrius has none)
   (c) Debugger stack walk past a non-leaf fn (still useful but
       not required for correctness)
   (d) ETW profilers
   (e) Windows error reporting dialogs that show a stack trace

   If a compiled cyrius fn faults, Windows will show the fault
   at the EIP with no unwind — acceptable for now. Leaves only
   PARTIALLY-AVAILABLE debugger support; core flow works.

SUMMARY — v5.5.5+ PRIORITY (subject to downstream pressure):
  1. struct-return-by-value (highest, first to break)
  2. .reloc + ASLR (medium, needed for DLLs)
  3. __chkstk (low, purely defensive)
  4. variadic float duplication (blocked on vararg lang feature)
  5. SEH (indefinite)
'''
